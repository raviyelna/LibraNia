---
phase: 03-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/schema.ts
  - electron/database/connection.ts
  - tests/database.test.ts
autonomous: true
requirements: [AI-05, AI-10]

must_haves:
  truths:
    - Conversations can be created and persisted with auto-generated titles
    - Messages can be stored with role (user/assistant/system), content, and provider metadata
    - Citations can be linked to messages with URL, title, snippet, and position
    - All conversation data survives app restart
  artifacts:
    - path: electron/database/schema.ts
      provides: conversations, messages, citations table definitions
      exports: [conversations, messages, citations]
    - path: electron/database/connection.ts
      provides: Schema initialization for new tables
      contains: CREATE TABLE IF NOT EXISTS conversations
  key_links:
    - from: messages table
      to: conversations table
      via: foreign key conversation_id with CASCADE delete
      pattern: references.*conversations.*onDelete.*cascade
    - from: citations table
      to: messages table
      via: foreign key message_id with CASCADE delete
      pattern: references.*messages.*onDelete.*cascade
---

<objective>
Extend database schema with conversations, messages, and citations tables to support persistent chat history with AI providers.

Purpose: Enable conversation persistence across app restarts, store message history with provider metadata, and link citations to AI responses for source tracking.

Output: Three new tables (conversations, messages, citations) integrated into existing Drizzle schema with proper foreign key relationships and cascade rules.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-RESEARCH.md
@.planning/phases/03-ai-integration/03-PATTERNS.md
@electron/database/schema.ts
@electron/database/connection.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add conversations, messages, and citations tables to Drizzle schema</name>
  <files>electron/database/schema.ts, tests/database.test.ts</files>
  
  <read_first>
    - electron/database/schema.ts (existing schema structure, import patterns, table definition patterns)
    - .planning/phases/03-ai-integration/03-PATTERNS.md (lines 36-70: schema extension pattern from Phase 2)
    - .planning/phases/03-ai-integration/03-RESEARCH.md (lines 506-539: Pattern 5 database schema extension)
  </read_first>
  
  <behavior>
    - Test 1: conversations table exists with id, title, created_at, updated_at columns
    - Test 2: messages table exists with id, conversation_id FK, role enum, content, provider_id, model, created_at
    - Test 3: citations table exists with id, message_id FK, url, title, snippet, position, created_at
    - Test 4: Foreign key cascade: deleting conversation deletes all messages
    - Test 5: Foreign key cascade: deleting message deletes all citations
    - Test 6: Role enum constraint: only 'user', 'assistant', 'system' allowed
  </behavior>
  
  <action>
Add three tables to electron/database/schema.ts following existing pattern (per D-09 persistent conversations, D-19 store citations):

**conversations table:**
- id: text (primary key, UUID)
- title: text (not null, auto-generated from first 50 chars per D-11)
- created_at: integer timestamp (not null)
- updated_at: integer timestamp (not null)

**messages table:**
- id: text (primary key, UUID)
- conversation_id: text (not null, FK to conversations.id with CASCADE delete)
- role: text enum ('user', 'assistant', 'system') (not null)
- content: text (not null, stores message body)
- provider_id: text (nullable, e.g., 'claude', 'openai', 'deepseek' per D-06)
- model: text (nullable, e.g., 'claude-3-5-sonnet-20241022' per D-23)
- created_at: integer timestamp (not null)

**citations table:**
- id: text (primary key, UUID)
- message_id: text (not null, FK to messages.id with CASCADE delete)
- url: text (not null, citation source URL per D-17)
- title: text (not null, citation title per D-18)
- snippet: text (nullable, excerpt from source)
- position: integer (not null, footnote number [1], [2], [3] per D-16)
- created_at: integer timestamp (not null)

Use sqliteTable, text(), integer() imports from drizzle-orm/sqlite-core. Follow foreign key pattern from noteTags table (lines 29-37 in schema.ts). Use { mode: 'timestamp' } for integer timestamps. Export all three tables.
  </action>
  
  <verify>
    <automated>npm test tests/database.test.ts -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - electron/database/schema.ts exports conversations, messages, citations tables
    - messages.role uses text() with enum constraint for 'user', 'assistant', 'system'
    - messages.conversation_id has references(() => conversations.id, { onDelete: 'cascade' })
    - citations.message_id has references(() => messages.id, { onDelete: 'cascade' })
    - All timestamp columns use integer('column_name', { mode: 'timestamp' }).notNull()
    - tests/database.test.ts has 6 new tests covering table structure and FK cascades
    - npm test tests/database.test.ts exits 0
  </acceptance_criteria>
  
  <done>Schema extended with three new tables, foreign key cascades verified, all tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Initialize new tables in database connection</name>
  <files>electron/database/connection.ts, tests/database.test.ts</files>
  
  <read_first>
    - electron/database/connection.ts (existing initDatabase function, table creation pattern)
    - electron/database/schema.ts (new table definitions from Task 1)
    - .planning/phases/03-ai-integration/03-PATTERNS.md (lines 36-70: schema extension pattern)
  </read_first>
  
  <behavior>
    - Test 1: initDatabase creates conversations table with correct columns
    - Test 2: initDatabase creates messages table with correct columns and FK
    - Test 3: initDatabase creates citations table with correct columns and FK
    - Test 4: Tables persist after database close and reopen
  </behavior>
  
  <action>
Update initDatabase() in electron/database/connection.ts to create the three new tables using raw SQL (following existing pattern for notes, tags, noteTags, links, noteVersions tables).

Add CREATE TABLE IF NOT EXISTS statements for:
1. conversations (id TEXT PRIMARY KEY, title TEXT NOT NULL, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL)
2. messages (id TEXT PRIMARY KEY, conversation_id TEXT NOT NULL REFERENCES conversations(id) ON DELETE CASCADE, role TEXT NOT NULL CHECK(role IN ('user', 'assistant', 'system')), content TEXT NOT NULL, provider_id TEXT, model TEXT, created_at INTEGER NOT NULL)
3. citations (id TEXT PRIMARY KEY, message_id TEXT NOT NULL REFERENCES messages(id) ON DELETE CASCADE, url TEXT NOT NULL, title TEXT NOT NULL, snippet TEXT, position INTEGER NOT NULL, created_at INTEGER NOT NULL)

Place after existing table creation statements, before FTS5 setup. Follow same pattern: db.exec() with multi-line SQL string.
  </action>
  
  <verify>
    <automated>npm test tests/database.test.ts -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - electron/database/connection.ts contains CREATE TABLE IF NOT EXISTS conversations
    - electron/database/connection.ts contains CREATE TABLE IF NOT EXISTS messages with CHECK(role IN (...))
    - electron/database/connection.ts contains CREATE TABLE IF NOT EXISTS citations
    - All three tables have REFERENCES clauses with ON DELETE CASCADE
    - tests/database.test.ts has 4 new tests verifying table creation and persistence
    - npm test tests/database.test.ts exits 0
  </acceptance_criteria>
  
  <done>Database initialization creates all three tables with proper constraints, verified by tests</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (IPC) | Conversation/message data crosses from untrusted renderer to main process |
| Main → Database | Conversation data persisted to SQLite (trusted local storage) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | messages.content | mitigate | Validate content length (max 1MB) in service layer (Plan 03-05) |
| T-03-02 | Denial of Service | Unlimited conversations | accept | Single-user desktop app, user controls their own data volume |
| T-03-03 | Tampering | SQL injection via conversation data | mitigate | Drizzle ORM parameterized queries exclusively (enforced in service layer) |
| T-03-04 | Information Disclosure | Deleted conversations in queries | mitigate | Soft delete pattern if needed (defer to Plan 03-05 based on requirements) |
| T-03-SC | Tampering | npm installs | mitigate | No new packages in this plan (schema extension only) |
</threat_model>

<verification>
## Phase-Level Checks

1. **Schema verification:** All three tables exist in schema.ts with correct exports
2. **Foreign key verification:** CASCADE delete rules properly configured
3. **Enum constraint:** messages.role restricted to 'user', 'assistant', 'system'
4. **Timestamp types:** All created_at/updated_at use integer with timestamp mode
5. **Database initialization:** Tables created on app startup
6. **Test coverage:** All table structures and FK cascades tested
</verification>

<success_criteria>
- User can create conversations that persist across app restarts
- Messages can be stored with role, content, and provider metadata
- Citations can be linked to messages with position tracking
- Deleting a conversation cascades to all messages and citations
- All database tests passing (existing + new tests)
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-01-SUMMARY.md` when done
</output>
