---
phase: 06-3d-visualization
plan: 10
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Graph/GraphSidePanel.tsx
autonomous: true
requirements: [VIZ-03]
gap_closure: true

must_haves:
  truths:
    - "Side panel width can be adjusted by dragging resize handle"
    - "Panel width persists during session (state maintained)"
  artifacts:
    - path: "src/components/Graph/GraphSidePanel.tsx"
      provides: "Resizable panel with drag handle"
      min_lines: 60
  key_links:
    - from: "GraphSidePanel resize handle"
      to: "panel width state"
      via: "mouse drag event handlers"
      pattern: "onMouseDown.*onMouseMove.*onMouseUp"
---

<objective>
Add drag-to-resize functionality to graph side panel.

Purpose: Side panel has fixed width, user cannot adjust (UAT Test 5, minor severity).

Output: Resizable side panel with drag handle on left edge.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-3d-visualization/06-UAT.md
@src/components/Graph/GraphSidePanel.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add resize handle and drag logic</name>
  <files>src/components/Graph/GraphSidePanel.tsx</files>
  <action>
Add resizable panel functionality to GraphSidePanel component:

**1. Add width state:**
```typescript
const [width, setWidth] = useState(384); // 384px = w-96 default
const [isResizing, setIsResizing] = useState(false);
```

**2. Add resize handle element:**
- Position on left edge of panel (absolute positioning)
- 4px wide, full height, cursor: col-resize
- Visual indicator: border-l-2 border-primary on hover
- Place before existing panel content

**3. Add mouse event handlers:**

onMouseDown on resize handle:
- Set isResizing to true
- Add document-level mousemove and mouseup listeners

onMouseMove (document level):
- Calculate new width: panel right edge - mouse X position
- Clamp width between 256px (min) and 768px (max)
- Update width state

onMouseUp (document level):
- Set isResizing to false
- Remove document-level listeners

**4. Update panel styling:**
- Replace fixed `w-96` with dynamic `style={{ width: `${width}px` }}`
- Add transition for smooth resize: `transition-none` when isResizing, `transition-all duration-150` otherwise

**5. Cleanup:**
- Remove document listeners on unmount
- Use useEffect for listener cleanup

Implementation notes:
- Use useRef for tracking resize state if needed
- Prevent text selection during resize (add `user-select: none` to body when isResizing)
- Consider adding visual feedback (change cursor globally during resize)
- Min width 256px ensures content remains readable
- Max width 768px prevents panel from dominating screen
  </action>
  <verify>
    <automated>npm test -- GraphSidePanel.test.tsx</automated>
  </verify>
  <done>GraphSidePanel has resize handle on left edge. Dragging handle adjusts panel width between 256px and 768px. Width state maintained during session. Event listeners cleaned up on unmount. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Mouse events → panel width | User drag gestures control panel dimensions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-10-01 | Denial of Service | Resize event handling | mitigate | Clamp width between 256px and 768px to prevent extreme values. Clean up listeners on unmount. |
| T-06-10-02 | Information Disclosure | Panel width state | accept | Panel width is UI state only, no sensitive data. Low risk. |
</threat_model>

<verification>
1. Run GraphSidePanel tests: `npm test -- GraphSidePanel.test.tsx`
2. Manual test: Click node to open panel, drag left edge to resize
3. Manual test: Verify width clamped between 256px and 768px
4. Manual test: Close and reopen panel, verify width resets to default (384px)
5. Verify no memory leaks (listeners cleaned up)
</verification>

<success_criteria>
- Side panel has visible resize handle on left edge
- Dragging handle adjusts panel width smoothly
- Width clamped between 256px (min) and 768px (max)
- Cursor changes to col-resize on hover
- Event listeners properly cleaned up on unmount
- All GraphSidePanel tests pass
- UAT Test 5 minor issue resolved
</success_criteria>

<output>
Create `.planning/phases/06-3d-visualization/06-10-SUMMARY.md` when done
</output>
