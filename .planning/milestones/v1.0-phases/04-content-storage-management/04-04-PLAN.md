---
phase: 04-content-storage-management
plan: 04
type: execute
wave: 3
depends_on:
  - 04-03
files_modified:
  - electron/ipc/content.handlers.ts
  - electron/main.ts
  - electron/preload.ts
  - tests/content.handlers.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05

must_haves:
  truths:
    - Renderer can invoke content operations via IPC
    - File upload uses Electron dialog API for secure path selection
    - Content operations exposed via window.api.content namespace
    - IPC handlers validate inputs and handle errors
  artifacts:
    - path: electron/ipc/content.handlers.ts
      provides: IPC handlers for content operations
      exports: ["registerContentHandlers"]
      min_lines: 100
    - path: electron/preload.ts
      provides: Content API exposed to renderer
      contains: "window.api.content"
    - path: tests/content.handlers.test.ts
      provides: IPC handler tests
      min_lines: 80
  key_links:
    - from: electron/ipc/content.handlers.ts
      to: electron/services/content.service.ts
      via: Service function imports
      pattern: "import.*createContent.*from.*content\\.service"
    - from: electron/main.ts
      to: electron/ipc/content.handlers.ts
      via: Handler registration
      pattern: "registerContentHandlers"
    - from: electron/preload.ts
      to: renderer process
      via: contextBridge.exposeInMainWorld
      pattern: "content:.*ipcRenderer\\.invoke"
---

<objective>
Expose content operations to renderer process via IPC handlers, integrate with Electron dialog API for secure file selection, and register handlers in main process.

Purpose: Provides secure communication bridge between renderer and main process for content operations, ensuring file access is controlled and validated.

Output: IPC handlers registered, content API exposed via window.api.content, file upload dialog integrated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-storage-management/04-CONTEXT.md
@.planning/phases/04-content-storage-management/04-RESEARCH.md
@.planning/phases/04-content-storage-management/04-03-PLAN.md
@electron/ipc/notes.handlers.ts
@electron/preload.ts
@electron/main.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create IPC handlers for content operations</name>
  <files>electron/ipc/content.handlers.ts, tests/content.handlers.test.ts</files>
  <read_first>
    - electron/ipc/notes.handlers.ts (IPC handler pattern: ipcMain.handle, logger.info, try/catch error handling, getORM() for database access)
    - .planning/phases/04-content-storage-management/04-CONTEXT.md (D-17 through D-19: service layer pattern, CRUD operations, window.api.content namespace)
    - .planning/phases/04-content-storage-management/04-RESEARCH.md (Pattern 5: secure IPC file access with path validation, file upload with Electron dialog API)
  </read_first>
  <behavior>
    - Test 1: content:upload handler shows file picker dialog and returns selected file path
    - Test 2: content:upload handler validates file type and size before processing
    - Test 3: content:create handler calls createContent service and returns Content record
    - Test 4: content:getById handler calls getContentById service and returns Content or null
    - Test 5: content:getAll handler calls getAllContent service and returns Content[] array
    - Test 6: content:delete handler calls deleteContent service and returns success boolean
    - Test 7: All handlers log operations and catch errors
  </behavior>
  <action>
Create electron/ipc/content.handlers.ts with the following structure:

**Imports:**
- ipcMain, dialog from electron
- logger from ../logger
- getORM from ../database/connection
- createContent, getContentById, updateContent, deleteContent, getAllContent from ../services/content.service

**Function: registerContentHandlers()**

Handler: content:upload
- Use dialog.showOpenDialog with properties: ['openFile']
- Set filters: [{ name: 'Documents', extensions: ['pdf', 'docx', 'txt', 'md'] }, { name: 'Images', extensions: ['png', 'jpg', 'jpeg', 'webp'] }] (per D-05, D-07)
- Return { canceled, filePath } where filePath is filePaths[0] or null if canceled
- Log operation: logger.info('IPC: content:upload', { canceled })
- Matches RESEARCH.md Pattern 5 for secure file selection

Handler: content:create
- Signature: async (event, data: { filePath: string; source: 'manual' | 'ai-generated'; confidence_score?: number; note_id?: string; message_id?: string })
- Call createContent(data, orm)
- Return created Content record
- Log operation: logger.info('IPC: content:create', { filePath: data.filePath, source: data.source })
- Catch errors: logger.error('content:create failed', error), throw error
- Matches notes.handlers.ts pattern

Handler: content:getById
- Signature: async (event, data: { id: string })
- Call getContentById(data.id, orm)
- Return Content or null
- Log operation: logger.info('IPC: content:getById', { id: data.id })
- Catch errors: logger.error('content:getById failed', error), throw error

Handler: content:getAll
- Signature: async (event)
- Call getAllContent(orm)
- Return Content[] array
- Log operation: logger.info('IPC: content:getAll')
- Catch errors: logger.error('content:getAll failed', error), throw error

Handler: content:delete
- Signature: async (event, data: { id: string })
- Call deleteContent(data.id, orm)
- Return { success: boolean }
- Log operation: logger.info('IPC: content:delete', { id: data.id })
- Catch errors: logger.error('content:delete failed', error), throw error

Handler: content:update
- Signature: async (event, data: { id: string; extracted_text?: string; thumbnail_path?: string; metadata?: string })
- Call updateContent(data.id, { extracted_text, thumbnail_path, metadata }, orm)
- Return updated Content record
- Log operation: logger.info('IPC: content:update', { id: data.id })
- Catch errors: logger.error('content:update failed', error), throw error

Log at end: logger.info('Content IPC handlers registered')

Export registerContentHandlers function.

Create tests/content.handlers.test.ts:
- Mock ipcMain, dialog, logger, getORM, content service functions
- Test each handler's behavior
- Verify logger calls
- Verify error handling
- Follow tests/notes.handlers.test.ts pattern if it exists, otherwise follow Vitest patterns from other test files
  </action>
  <verify>
    <automated>npm test tests/content.handlers.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - electron/ipc/content.handlers.ts contains `export function registerContentHandlers`
    - content:upload handler uses dialog.showOpenDialog with file filters
    - content:create handler calls createContent service
    - content:getById handler calls getContentById service
    - content:getAll handler calls getAllContent service
    - content:delete handler calls deleteContent service
    - content:update handler calls updateContent service
    - All handlers log operations and catch errors
    - tests/content.handlers.test.ts exits 0 with all assertions passing
  </acceptance_criteria>
  <done>IPC handlers for content operations implemented with file dialog integration, logging, and error handling, tests passing</done>
</task>

<task type="auto">
  <name>Task 2: Register content handlers and expose API to renderer</name>
  <files>electron/main.ts, electron/preload.ts</files>
  <read_first>
    - electron/main.ts (existing handler registration pattern: registerNotesHandlers, registerTagsHandlers, registerSearchHandlers, registerExportHandlers, registerAIHandlers after database initialization)
    - electron/preload.ts (existing API exposure pattern: window.api.notes, window.api.tags, window.api.search, window.api.export, window.api.chat)
    - .planning/phases/04-content-storage-management/04-CONTEXT.md (D-19: expose via window.api.content namespace)
  </read_first>
  <action>
Update electron/main.ts:
- Add import: import { registerContentHandlers } from './ipc/content.handlers.js'
- After registerAIHandlers() call (around line 150-160), add: registerContentHandlers()
- Placement: after database initialization (initDatabase) and before window creation, grouped with other handler registrations

Update electron/preload.ts:
- Add content namespace to window.api object after conversation operations (around line 114):

```typescript
// Content operations
content: {
  upload: () => ipcRenderer.invoke('content:upload'),
  create: (data: {
    filePath: string;
    source: 'manual' | 'ai-generated';
    confidence_score?: number;
    note_id?: string;
    message_id?: string;
  }) => ipcRenderer.invoke('content:create', data),
  getById: (id: string) => ipcRenderer.invoke('content:getById', { id }),
  getAll: () => ipcRenderer.invoke('content:getAll'),
  update: (data: {
    id: string;
    extracted_text?: string;
    thumbnail_path?: string;
    metadata?: string;
  }) => ipcRenderer.invoke('content:update', data),
  delete: (id: string) => ipcRenderer.invoke('content:delete', { id }),
},
```

Matches existing window.api.notes, window.api.tags patterns per D-19.
  </action>
  <verify>
    <automated>grep -c "registerContentHandlers" electron/main.ts && grep -c "content:" electron/preload.ts</automated>
  </verify>
  <acceptance_criteria>
    - electron/main.ts contains `import { registerContentHandlers }`
    - electron/main.ts calls registerContentHandlers() after database initialization
    - electron/preload.ts contains `content: {` namespace in window.api
    - electron/preload.ts exposes upload, create, getById, getAll, update, delete operations
    - grep confirms both files updated (exit 0)
  </acceptance_criteria>
  <done>Content handlers registered in main process, API exposed to renderer via window.api.content namespace</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (IPC) | Content operations invoked from untrusted renderer |
| Main → Filesystem | File paths validated before filesystem access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-11 | Tampering | IPC data validation | mitigate | Service layer validates file paths, MIME types, and sizes before processing |
| T-04-12 | Information Disclosure | File path exposure | mitigate | dialog.showOpenDialog provides secure path selection, no direct path input from renderer |
| T-04-13 | Elevation of Privilege | Arbitrary file access | mitigate | File operations restricted to /content/ directory, path normalization in service layer |
</threat_model>

<verification>
## Phase-Level Checks

After all tasks complete:
1. Content handlers registered in main.ts
2. Content API exposed in preload.ts
3. File upload uses Electron dialog API
4. All IPC handlers log operations
5. Error handling implemented for all handlers
6. Tests passing for IPC handlers
</verification>

<success_criteria>
- [x] registerContentHandlers function implemented and exported
- [x] content:upload, content:create, content:getById, content:getAll, content:update, content:delete handlers registered
- [x] registerContentHandlers called in main.ts after database initialization
- [x] window.api.content namespace exposed in preload.ts
- [x] All IPC handler tests passing
</success_criteria>

<output>
Create `.planning/phases/04-content-storage-management/04-04-SUMMARY.md` when done
</output>
