---
phase: 04-content-storage-management
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/schema.ts
  - electron/database/fts.ts
  - tests/content.schema.test.ts
autonomous: true
requirements:
  - CONT-01
  - CONT-02
  - CONT-03
  - CONT-04
  - CONT-05
  - CONT-06

must_haves:
  truths:
    - Content metadata stored in SQLite with UUID primary keys
    - Document text indexed in FTS5 for search integration
    - Content can be linked to notes and messages via foreign keys
    - Tags can be applied to content via junction table
  artifacts:
    - path: electron/database/schema.ts
      provides: content and content_tags table definitions
      exports: ["content", "contentTags"]
    - path: electron/database/fts.ts
      provides: content_fts FTS5 table with triggers
      contains: "CREATE VIRTUAL TABLE IF NOT EXISTS content_fts"
    - path: tests/content.schema.test.ts
      provides: Schema validation tests
      min_lines: 50
  key_links:
    - from: electron/database/schema.ts
      to: notes table
      via: content.note_id foreign key
      pattern: "references.*notes\\.id"
    - from: electron/database/schema.ts
      to: messages table
      via: content.message_id foreign key
      pattern: "references.*messages\\.id"
    - from: electron/database/fts.ts
      to: content table
      via: FTS5 triggers
      pattern: "content_fts_insert.*INSERT ON content"
---

<objective>
Extend database schema to support diverse content types (documents, images) with rich metadata, FTS5 indexing for document text, and relationships to notes/messages/tags.

Purpose: Provides data layer foundation for storing user-uploaded documents/images and AI-generated content with searchable text and flexible metadata.

Output: Database schema with content table, content_tags junction, and content_fts FTS5 table with automated triggers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-content-storage-management/04-CONTEXT.md
@.planning/phases/04-content-storage-management/04-RESEARCH.md
@electron/database/schema.ts
@electron/database/fts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend database schema with content and content_tags tables</name>
  <files>electron/database/schema.ts, tests/content.schema.test.ts</files>
  <read_first>
    - electron/database/schema.ts (existing schema patterns: UUID primary keys, timestamp columns, foreign keys with CASCADE/SET NULL, metadata as JSON text column)
    - .planning/phases/04-content-storage-management/04-CONTEXT.md (D-09 through D-15: source enum, confidence_score, metadata JSON, note_id/message_id foreign keys, SET NULL on note deletion)
    - .planning/phases/04-content-storage-management/04-RESEARCH.md (content table structure, foreign key patterns)
  </read_first>
  <behavior>
    - Test 1: content table exists with columns: id (text PK), file_path (text NOT NULL), thumbnail_path (text nullable), mime_type (text NOT NULL), original_filename (text NOT NULL), file_size (integer NOT NULL), extracted_text (text nullable), source (text NOT NULL), confidence_score (real nullable), metadata (text nullable), note_id (text nullable FK to notes.id), message_id (text nullable FK to messages.id), created_at (timestamp NOT NULL), updated_at (timestamp NOT NULL)
    - Test 2: content.note_id foreign key references notes.id with onDelete: 'set null' (per D-15)
    - Test 3: content.message_id foreign key references messages.id with onDelete: 'cascade' (AI-generated content deleted with message)
    - Test 4: content_tags junction table exists with content_id and tag_id columns, both CASCADE delete (per D-12)
    - Test 5: content_tags has composite primary key on (content_id, tag_id)
  </behavior>
  <action>
Add content table to electron/database/schema.ts after citations table:
- id: text().primaryKey() — UUID generated via crypto.randomUUID()
- file_path: text().notNull() — relative path from project root, format: content/{uuid}.{ext} (per D-03)
- thumbnail_path: text() — nullable, format: content/thumbnails/{uuid}.jpg
- mime_type: text().notNull() — validated MIME type from file-type library
- original_filename: text().notNull() — preserves user's original filename for display
- file_size: integer().notNull() — bytes, for validation and UI display
- extracted_text: text() — nullable, populated for PDF/DOCX/TXT/MD via pdf-parse/mammoth (per D-06)
- source: text().notNull() — enum 'manual' | 'ai-generated' (per D-09)
- confidence_score: real() — nullable, 0.0-1.0 for AI-generated content (per D-10, satisfies CONT-06)
- metadata: text() — nullable JSON blob for extensibility (per D-11, matches notes.metadata pattern)
- note_id: text().references(() => notes.id, { onDelete: 'set null' }) — nullable, per D-14 and D-15
- message_id: text().references(() => messages.id, { onDelete: 'cascade' }) — nullable, AI-generated content linked to message
- created_at: integer('created_at', { mode: 'timestamp' }).notNull()
- updated_at: integer('updated_at', { mode: 'timestamp' }).notNull()

Add contentTags junction table after content table:
- content_id: text().notNull().references(() => content.id, { onDelete: 'cascade' })
- tag_id: text().notNull().references(() => tags.id, { onDelete: 'cascade' })
- Composite primary key: { columns: [table.content_id, table.tag_id] } (matches noteTags pattern per D-12)

Export both tables: export const content, export const contentTags

Create tests/content.schema.test.ts following existing tests/notes.service.test.ts pattern with describe blocks for schema validation.
  </action>
  <verify>
    <automated>npm test tests/content.schema.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - electron/database/schema.ts contains `export const content = sqliteTable('content'`
    - electron/database/schema.ts contains `export const contentTags = sqliteTable('content_tags'`
    - content table has 14 columns matching specification above
    - content.note_id references notes.id with onDelete: 'set null'
    - content.message_id references messages.id with onDelete: 'cascade'
    - contentTags has composite PK on (content_id, tag_id)
    - tests/content.schema.test.ts exits 0 with all assertions passing
  </acceptance_criteria>
  <done>Database schema extended with content and content_tags tables, all foreign key relationships validated, tests passing</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create FTS5 virtual table for document text search</name>
  <files>electron/database/fts.ts, tests/content.fts.test.ts</files>
  <read_first>
    - electron/database/fts.ts (existing FTS5 pattern: notes_fts with triggers for INSERT/UPDATE/DELETE, tokenize='porter unicode61 remove_diacritics 2')
    - .planning/phases/04-content-storage-management/04-CONTEXT.md (D-06: extract text and index in FTS5)
    - .planning/phases/04-content-storage-management/04-RESEARCH.md (content_fts table structure, trigger patterns)
  </read_first>
  <behavior>
    - Test 1: content_fts virtual table created with columns: original_filename, extracted_text
    - Test 2: INSERT trigger populates content_fts when content row inserted
    - Test 3: UPDATE trigger updates content_fts when content.extracted_text or content.original_filename changes
    - Test 4: DELETE trigger removes content_fts row when content row deleted
    - Test 5: FTS5 search on content_fts returns ranked results using BM25
  </behavior>
  <action>
Add setupContentFTS5 function to electron/database/fts.ts after setupNotesFTS5:
- Create content_fts virtual table using fts5(original_filename, extracted_text, content='content', content_rowid='rowid', tokenize='porter unicode61 remove_diacritics 2') — matches notes_fts tokenizer per existing pattern
- Create content_fts_insert trigger AFTER INSERT ON content: INSERT INTO content_fts(rowid, original_filename, extracted_text) VALUES (new.rowid, new.original_filename, new.extracted_text)
- Create content_fts_update trigger AFTER UPDATE ON content: UPDATE content_fts SET original_filename = new.original_filename, extracted_text = new.extracted_text WHERE rowid = new.rowid
- Create content_fts_delete trigger AFTER DELETE ON content: DELETE FROM content_fts WHERE rowid = old.rowid
- Export setupContentFTS5 function

Call setupContentFTS5(db) in electron/database/connection.ts initDatabase function after setupNotesFTS5(db)

Create tests/content.fts.test.ts following tests/search.service.test.ts pattern with describe blocks for FTS5 operations.
  </action>
  <verify>
    <automated>npm test tests/content.fts.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - electron/database/fts.ts contains `export function setupContentFTS5`
    - content_fts virtual table uses porter unicode61 tokenizer (matches notes_fts)
    - Three triggers created: content_fts_insert, content_fts_update, content_fts_delete
    - electron/database/connection.ts calls setupContentFTS5(db) after setupNotesFTS5
    - tests/content.fts.test.ts exits 0 with FTS5 search returning ranked results
  </acceptance_criteria>
  <done>FTS5 virtual table for content search created with automated triggers, integrated into database initialization, tests passing</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (IPC) | File paths and metadata cross from untrusted renderer to main process |
| Filesystem → Database | File content and metadata stored in database after validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | content.file_path | mitigate | Path validation in service layer (Task 2 of Plan 04-02): normalize paths, reject traversal attempts |
| T-04-02 | Information Disclosure | content.extracted_text | accept | Text extraction is feature requirement, no PII expected in documents |
| T-04-03 | Denial of Service | FTS5 index bloat | mitigate | Truncate extracted_text to 100KB before indexing (per RESEARCH.md Pitfall 4) |
| T-04-SC | Tampering | npm installs (sharp, pdf-parse, mammoth, file-type) | mitigate | Human verification checkpoint in Plan 04-02 before package installation |
</threat_model>

<verification>
## Phase-Level Checks

After all tasks complete:
1. Database schema includes content and content_tags tables
2. FTS5 content_fts table created with triggers
3. All tests in tests/content.schema.test.ts and tests/content.fts.test.ts pass
4. Foreign key relationships validated (note_id SET NULL, message_id CASCADE)
5. Content can be tagged via content_tags junction table
</verification>

<success_criteria>
- [x] content table exists with 14 columns including file_path, mime_type, extracted_text, source, confidence_score, metadata
- [x] content_tags junction table exists with CASCADE delete on both foreign keys
- [x] content_fts FTS5 virtual table created with triggers for INSERT/UPDATE/DELETE
- [x] setupContentFTS5 called in database initialization
- [x] All schema tests passing
- [x] All FTS5 tests passing
</success_criteria>

<output>
Create `.planning/phases/04-content-storage-management/04-01-SUMMARY.md` when done
</output>
