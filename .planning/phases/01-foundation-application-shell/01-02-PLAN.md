---
phase: 01-foundation-application-shell
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/App.tsx
  - src/components/Layout/Sidebar.tsx
  - src/components/Layout/MainContent.tsx
  - src/contexts/ThemeContext.tsx
  - src/hooks/useTheme.ts
  - src/index.css
  - src/types/theme.ts
autonomous: true
requirements:
  - APP-04
must_haves:
  truths:
    - User can toggle between light and dark mode
    - Theme preference persists across app restarts
    - Theme follows system preference on first launch
    - Sidebar navigation displays with collapsible behavior
  artifacts:
    - path: src/contexts/ThemeContext.tsx
      provides: Theme state management with React Context
      exports: ["ThemeProvider", "useTheme"]
    - path: src/components/Layout/Sidebar.tsx
      provides: Collapsible sidebar navigation
      min_lines: 50
    - path: src/index.css
      provides: CSS custom properties for theme colors
      contains: "--color-"
  key_links:
    - from: src/App.tsx
      to: src/contexts/ThemeContext.tsx
      via: ThemeProvider wraps application
      pattern: <ThemeProvider>
    - from: src/components/Layout/Sidebar.tsx
      to: src/hooks/useTheme.ts
      via: Theme toggle button uses theme hook
      pattern: useTheme\(\)
---

<objective>
Implement theme system (light/dark mode) and navigation structure with collapsible sidebar.

Purpose: Establish UI foundation per D-14 (hybrid theme approach), D-15 (system preference default), D-16 (theme persistence), D-11 (sidebar navigation), D-12 (collapsible sidebar).

Output: Functional theme switching and navigation layout ready for feature routes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-application-shell/01-CONTEXT.md
@.planning/phases/01-foundation-application-shell/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement theme system with CSS custom properties and Context API</name>
  <files>
    src/contexts/ThemeContext.tsx
    src/hooks/useTheme.ts
    src/types/theme.ts
    src/index.css
  </files>
  <behavior>
    - Theme context provides current theme ('light' | 'dark' | 'system')
    - toggleTheme() cycles through light → dark → system
    - setTheme(theme) sets specific theme
    - On mount, reads system preference via window.matchMedia('(prefers-color-scheme: dark)')
    - Theme changes apply CSS custom properties and Tailwind dark mode class
    - Theme preference persists to localStorage (preparation for D-16 config file in Plan 04)
  </behavior>
  <action>
    Create theme system implementing D-14 (hybrid CSS custom properties + Tailwind dark mode), D-15 (system preference default), D-16 (persistence).

    Create src/types/theme.ts:
    - Export type Theme = 'light' | 'dark' | 'system'
    - Export interface ThemeContextValue with theme, effectiveTheme, setTheme, toggleTheme

    Create src/contexts/ThemeContext.tsx:
    - Create ThemeContext with React.createContext per D-05 (Context + hooks)
    - ThemeProvider component manages theme state
    - On mount: read localStorage 'theme' key, fallback to 'system'
    - If theme is 'system', detect via window.matchMedia('(prefers-color-scheme: dark)')
    - Listen to system theme changes with matchMedia.addEventListener('change')
    - Apply theme by setting document.documentElement.classList ('dark' class for dark mode)
    - Save theme to localStorage on change
    - Provide { theme, effectiveTheme, setTheme, toggleTheme }

    Create src/hooks/useTheme.ts:
    - Export useTheme hook that calls useContext(ThemeContext)
    - Throw error if used outside ThemeProvider

    Update src/index.css (extends @theme from Plan 01):
    - Ensure @theme directive contains both light and dark color definitions
    - Light theme colors in root @theme block
    - Dark theme colors in .dark selector within @theme block
    - Variables: --color-background, --color-foreground, --color-primary, --color-secondary, --color-border, --color-muted
    - Light theme: white backgrounds, dark text
    - Dark theme: dark backgrounds, light text

    Note: Tailwind v4 dark mode uses class strategy automatically when .dark class is present on html element.
  </action>
  <verify>
    <automated>npm run build && grep -q "ThemeProvider" src/contexts/ThemeContext.tsx && grep -q "@theme" src/index.css</automated>
  </verify>
  <done>Theme context provides theme state, CSS custom properties defined for both themes, Tailwind configured for dark mode</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create collapsible sidebar navigation component</name>
  <files>
    src/components/Layout/Sidebar.tsx
    src/components/Layout/MainContent.tsx
    src/components/Layout/Layout.tsx
    src/components/ui/Button.tsx
    src/App.tsx
  </files>
  <behavior>
    - Sidebar displays on left side with fixed width (expanded: 240px, collapsed: 64px)
    - Collapse button toggles between expanded and icon-only states
    - Sidebar state persists to localStorage
    - Navigation items show icon + label when expanded, icon only when collapsed
    - Smooth transition animation between states (200ms)
    - Main content area adjusts width based on sidebar state
  </behavior>
  <action>
    Create navigation layout implementing D-11 (sidebar navigation), D-12 (collapsible sidebar), D-13 (React Router preparation).

    Install dependencies:
    - react-router-dom ^7.15.1 (for D-13)
    - lucide-react ^0.460.0 (icon library, lightweight alternative to react-icons)

    Create src/components/ui/Button.tsx:
    - Reusable button component with variants (default, ghost, outline)
    - Uses Tailwind classes for styling
    - TypeScript props: variant, size, children, onClick, className
    - Accessible: proper button semantics, focus styles

    Create src/components/Layout/Sidebar.tsx:
    - Functional component with collapsed state (useState)
    - Read initial collapsed state from localStorage 'sidebar-collapsed'
    - Render sidebar with fixed positioning, left: 0, height: 100vh
    - Width: collapsed ? 64px : 240px with transition-all duration-200
    - Collapse toggle button at top (ChevronLeft/ChevronRight icon from lucide-react)
    - Navigation items array: [{ icon: Home, label: 'Home', path: '/' }, { icon: Library, label: 'Library', path: '/library' }, { icon: Settings, label: 'Settings', path: '/settings' }]
    - Map navigation items to buttons with icon + label (label hidden when collapsed)
    - Theme toggle button at bottom using useTheme hook (Sun/Moon icon)
    - Save collapsed state to localStorage on toggle
    - Use Tailwind classes: bg-background, border-r, border-border

    Create src/components/Layout/MainContent.tsx:
    - Functional component accepting children
    - Render main content area with margin-left matching sidebar width
    - Transition margin-left to match sidebar animation
    - Use Tailwind: flex-1, p-6, bg-background

    Create src/components/Layout/Layout.tsx:
    - Wrapper component combining Sidebar + MainContent
    - Use React Router's Outlet for nested routes (preparation for D-13)
    - Flex layout: sidebar fixed, main content fills remaining space

    Update src/App.tsx:
    - Wrap with ThemeProvider from Task 1
    - Wrap with BrowserRouter from react-router-dom
    - Use Layout component
    - Define Routes: / (home placeholder), /library (placeholder), /settings (placeholder)
    - Each route renders simple placeholder component with heading
  </action>
  <verify>
    <automated>npm run build && grep -q "Sidebar" src/components/Layout/Sidebar.tsx && grep -q "BrowserRouter" src/App.tsx</automated>
  </verify>
  <done>Sidebar renders with collapse functionality, navigation items display correctly, theme toggle button works, React Router configured with placeholder routes</done>
</task>

<task type="auto">
  <name>Task 3: Defer Radix UI installation to Plan 03 (used for restart confirmation dialog)</name>
  <files>
    package.json
  </files>
  <action>
    Defer Radix UI installation per verification feedback (Blocker 4: Radix UI unused in Plan 02).

    Radix UI will be installed in Plan 03 where it's actually used:
    - @radix-ui/react-dialog for restart confirmation dialog (mode switching)
    - @radix-ui/react-dropdown-menu for system tray menu (if needed in renderer)

    This task is a placeholder to document the deferral decision. No action required in Plan 02.

    Rationale: Installing libraries before they're used adds unnecessary dependencies and increases bundle size. Plan 03 Task 2 creates the restart confirmation dialog, which is the first actual use of Radix Dialog.
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Radix UI installation deferred to Plan 03 where it's first used</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| localStorage → Application | Theme and sidebar preferences read from browser storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Tampering | localStorage theme/sidebar state | accept | Low risk - user can only affect their own preferences, no security impact |
| T-01-05 | Denial of Service | CSS custom properties | accept | Malformed CSS variables fail gracefully with browser defaults |
</threat_model>

<verification>
## Phase 1 Plan 02 Verification

After all tasks complete:

1. **Theme toggle:** Click theme button cycles through light → dark → system
2. **Theme persistence:** Refresh app, theme preference persists
3. **System theme:** Set theme to 'system', change OS theme, app follows
4. **Sidebar collapse:** Click collapse button, sidebar animates to icon-only
5. **Sidebar persistence:** Refresh app, sidebar state persists
6. **Navigation:** Click navigation items, routes change (placeholder content displays)
7. **Accessibility:** Tab through UI, focus indicators visible, screen reader announces elements
</verification>

<success_criteria>
- User can toggle between light and dark mode (APP-04 complete)
- Theme preference persists across app restarts
- Theme follows system preference when set to 'system'
- Sidebar navigation displays with collapsible behavior
- Navigation structure ready for feature routes
- Radix UI deferred to Plan 03 (no unused dependencies)
</success_criteria>

<output>
Create `.planning/phases/01-foundation-application-shell/01-02-SUMMARY.md` when done
</output>
