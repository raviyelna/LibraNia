---
phase: 01-design-system-foundation
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md
autonomous: true
requirements:
  - UI-FOUND-01
  - UI-FOUND-02
  - UI-FOUND-03
  - UI-FOUND-04
  - UI-FOUND-05
  - UI-FOUND-06

must_haves:
  truths:
    - "Design system documentation exists with complete color palette reference"
    - "Typography scale and font usage guidelines are documented"
    - "Spacing system with 8px grid is explained with examples"
    - "Animation guidelines include reduced-motion considerations"
    - "Accessibility standards (WCAG 2.1 AA) are documented with verification steps"
    - "Code examples show how to use design tokens in components"
  artifacts:
    - path: ".planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md"
      provides: "Complete design system documentation"
      min_lines: 150
      contains: "Color Palette"
  key_links:
    - from: "DESIGN-SYSTEM.md"
      to: "src/index.css"
      via: "References design token definitions"
      pattern: "src/index.css"
    - from: "DESIGN-SYSTEM.md"
      to: "src/components/ui/Button.tsx"
      via: "Shows reference implementation"
      pattern: "Button.tsx"

---

<objective>
Create comprehensive design system documentation that serves as the single source of truth for all UI development, including color palette, typography, spacing, animations, accessibility guidelines, and usage examples.

**Purpose:** Enable developers to build consistent, accessible UI components by providing clear guidelines, code examples, and best practices. Documentation ensures design system adoption and prevents inconsistent implementations.

**Output:** DESIGN-SYSTEM.md with complete reference guide including visual examples, code snippets, accessibility requirements, and common patterns.
</objective>

<execution_context>
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\.claude\get-shit-done\workflows\execute-plan.md
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\.planning\phases\01-design-system-foundation\01-CONTEXT.md
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\.planning\phases\01-design-system-foundation\01-RESEARCH.md
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\.planning\phases\01-design-system-foundation\01-01-PLAN.md
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\src\index.css
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\src\styles\utilities.css
@D:\sourcecode\vibe\LibraNia_v2\LibraNia\src\components\ui\Button.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document color palette and usage guidelines</name>
  <files>.planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md</files>
  <action>
    Create DESIGN-SYSTEM.md with comprehensive color palette documentation:

    **Section 1: Color Palette**
    - Document neon accent colors per D-01: Blue (#3B82F6 / rgb(59 130 246)), Blue Light (#60A5FA for text), Cyan (#06B6D4), Purple (#A855F7)
    - Document semantic colors per D-04: Success (#10B981), Warning (#FBBF24), Error (#EF4444)
    - Document dark theme colors per D-03: Background (#0F172A), Foreground (#F8FAFC), Muted (#1E293B), Border (#334155)
    - Include hex codes, RGB values, and Tailwind utility class names (bg-neon-blue, text-neon-cyan, etc.)
    - Show color swatches with visual representation (use markdown color blocks or describe appearance)

    **Section 2: Color Usage Guidelines**
    - When to use each neon color: Blue for primary actions, Cyan for secondary/info, Purple for highlights/special states
    - Semantic color usage: Success for confirmations, Warning for caution, Error for failures
    - Contrast requirements: Reference WCAG 2.1 AA standards (4.5:1 for text, 3:1 for UI components)
    - Accessibility note: Use neon-blue-light (#60A5FA) for text, saturated neons for accents/borders only
    - Code examples showing correct color application in components

    **Section 3: Glow Effects**
    - Document glow utilities per D-02: .glow-sm (subtle), .glow-md (medium), .glow-purple (purple variant), .glow-pulse (animated)
    - When to use glows: Interactive elements on hover/focus, not static content
    - Performance considerations: Limit to 2-3 shadow layers, use opacity 0.3-0.5
    - Reduced motion: Note that glows are disabled for prefers-reduced-motion users
    - Code examples with Button component showing glow application
  </action>
  <verify>
    <automated>
      # Verify file exists
      test -f .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ DESIGN-SYSTEM.md created" || echo "✗ File missing"
      
      # Verify color palette section
      grep -q "Color Palette" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Color palette section present" || echo "✗ Missing color palette"
      
      # Verify all neon colors documented
      grep -q "#3B82F6" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Neon blue documented" || echo "✗ Missing neon blue"
      grep -q "#06B6D4" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Neon cyan documented" || echo "✗ Missing neon cyan"
      grep -q "#A855F7" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Neon purple documented" || echo "✗ Missing neon purple"
      
      # Verify usage guidelines
      grep -q "Usage" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Usage guidelines present" || echo "✗ Missing usage guidelines"
      
      # Verify glow effects documented
      grep -q "glow-sm" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Glow effects documented" || echo "✗ Missing glow documentation"
      
      # Verify WCAG references
      grep -q "WCAG" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ WCAG standards referenced" || echo "✗ Missing WCAG reference"
    </automated>
  </verify>
  <done>
    Color palette fully documented with hex codes, RGB values, Tailwind utilities, and visual descriptions. Usage guidelines explain when to use each color with accessibility considerations. Glow effects documented with performance and reduced-motion notes. Code examples show proper implementation.
  </done>
</task>

<task type="auto">
  <name>Task 2: Document typography, spacing, and animation systems</name>
  <files>.planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md</files>
  <action>
    Extend DESIGN-SYSTEM.md with typography, spacing, and animation documentation:

    **Section 4: Typography System**
    - Document font families: Inter (body text, UI elements), Space Grotesk (headings, display text)
    - Document font size scale (modular scale 1.25 ratio): xs (12px), sm (14px), base (16px), lg (20px), xl (24px), 2xl (30px), 3xl (36px)
    - Document font weights: normal (400), medium (500), semibold (600), bold (700)
    - Show Tailwind utility classes: font-sans, font-display, text-xs through text-3xl, font-normal through font-bold
    - Include typography hierarchy examples: h1 (text-3xl font-bold), h2 (text-2xl font-semibold), body (text-base), caption (text-sm text-muted)
    - Code examples showing proper heading and body text usage

    **Section 5: Spacing System**
    - Document 8px base grid per D-03: Tailwind default scale (4, 8, 12, 16, 20, 24, 32, 40, 48, 64px)
    - Document custom spacing extensions: spacing-18 (72px), spacing-22 (88px)
    - Show Tailwind utility classes: space-2 (8px), space-4 (16px), space-6 (24px), etc.
    - Usage guidelines: Use 8px increments for consistency, avoid arbitrary values unless necessary
    - Code examples showing spacing in layouts (padding, margin, gap)

    **Section 6: Animation System**
    - Document transition timing per D-05: fast (150ms), base (200ms)
    - Document easing curve per D-06: cubic-bezier(0.4, 0, 0.2, 1) — Material Design standard
    - Document micro-interactions per D-07: glow pulse on hover, fade-in on mount, slide-in for panels
    - Show Tailwind utility classes: transition-fast, transition-base, duration-fast, duration-base
    - Reduced motion per D-08: All animations disabled for prefers-reduced-motion users, use motion-reduce: and motion-safe: variants
    - Code examples showing proper animation usage with reduced-motion fallbacks
  </action>
  <verify>
    <automated>
      # Verify typography section
      grep -q "Typography" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Typography section present" || echo "✗ Missing typography"
      grep -q "Inter" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Inter font documented" || echo "✗ Missing Inter font"
      grep -q "Space Grotesk" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Space Grotesk documented" || echo "✗ Missing Space Grotesk"
      
      # Verify spacing section
      grep -q "Spacing" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Spacing section present" || echo "✗ Missing spacing"
      grep -q "8px" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ 8px grid documented" || echo "✗ Missing 8px grid"
      
      # Verify animation section
      grep -q "Animation" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Animation section present" || echo "✗ Missing animation"
      grep -q "150ms" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Transition timing documented" || echo "✗ Missing transition timing"
      grep -q "prefers-reduced-motion" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Reduced motion documented" || echo "✗ Missing reduced motion"
      
      # Verify code examples present
      grep -c '```' .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md | awk '$1 >= 6 {print "✓ Code examples present (" $1/2 " blocks)"} $1 < 6 {print "✗ Insufficient code examples"}'
    </automated>
  </verify>
  <done>
    Typography system documented with font families, size scale, weights, and hierarchy examples. Spacing system documented with 8px grid and Tailwind utilities. Animation system documented with timing, easing, micro-interactions, and reduced-motion support. All sections include code examples and usage guidelines.
  </done>
</task>

<task type="auto">
  <name>Task 3: Document responsive breakpoints, accessibility standards, and reference implementation</name>
  <files>.planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md</files>
  <action>
    Complete DESIGN-SYSTEM.md with responsive design, accessibility, and implementation guidance:

    **Section 7: Responsive Breakpoints**
    - Document Tailwind default breakpoints per UI-FOUND-05: sm (640px), md (768px), lg (1024px), xl (1280px), 2xl (1536px)
    - Explain mobile-first approach: Base styles for mobile, use sm:/md:/lg: prefixes for larger screens
    - Show responsive utility examples: text-base sm:text-lg md:text-xl, px-4 sm:px-6 md:px-8
    - Layout examples: grid-cols-1 sm:grid-cols-2 lg:grid-cols-3
    - Code examples showing responsive component patterns

    **Section 8: Accessibility Standards (WCAG 2.1 AA)**
    - Document contrast requirements per UI-FOUND-06: 4.5:1 for normal text, 3:1 for large text and UI components
    - Document keyboard navigation requirements: All interactive elements must be keyboard accessible, visible focus states required
    - Document focus state implementation: focus-visible:ring-2 focus-visible:ring-neon-blue focus-visible:ring-offset-2
    - Document ARIA requirements: Proper semantic HTML, ARIA labels for icon-only buttons, ARIA live regions for dynamic content
    - Document reduced motion support: All animations must have motion-reduce: fallbacks
    - Include verification checklist: Contrast checker tools, keyboard navigation testing, screen reader testing

    **Section 9: Reference Implementation**
    - Show Button component as reference implementation with all design patterns
    - Include complete Button.tsx code example showing: neon colors, glow effects, transitions, focus states, reduced motion, responsive sizing
    - Explain how to apply design system patterns to new components
    - Include common patterns: hover states, focus states, disabled states, loading states
    - Link to source files: src/index.css (design tokens), src/styles/utilities.css (glow effects), src/components/ui/Button.tsx (reference component)

    **Section 10: Quick Reference**
    - Create quick reference table with most common utilities: colors, spacing, typography, transitions
    - Include "Do's and Don'ts" section: Use neon-blue-light for text (not neon-blue), apply glows to interactive elements only, always include motion-reduce variants
    - Include troubleshooting section: Font not loading (check preconnect), glow not showing (check reduced motion), colors not applying (check RGB format)
  </action>
  <verify>
    <automated>
      # Verify responsive section
      grep -q "Responsive\|Breakpoint" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Responsive section present" || echo "✗ Missing responsive"
      grep -q "640px\|sm:" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Breakpoints documented" || echo "✗ Missing breakpoints"
      
      # Verify accessibility section
      grep -q "Accessibility\|WCAG" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Accessibility section present" || echo "✗ Missing accessibility"
      grep -q "4.5:1" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Contrast ratios documented" || echo "✗ Missing contrast ratios"
      grep -q "keyboard" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Keyboard navigation documented" || echo "✗ Missing keyboard nav"
      
      # Verify reference implementation
      grep -q "Reference\|Implementation" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Reference section present" || echo "✗ Missing reference"
      grep -q "Button" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Button example included" || echo "✗ Missing Button example"
      
      # Verify quick reference
      grep -q "Quick Reference\|Do's and Don'ts" .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md && echo "✓ Quick reference present" || echo "✗ Missing quick reference"
      
      # Verify minimum length (comprehensive documentation)
      wc -l .planning/phases/01-design-system-foundation/DESIGN-SYSTEM.md | awk '$1 >= 150 {print "✓ Documentation comprehensive (" $1 " lines)"} $1 < 150 {print "✗ Documentation too brief (" $1 " lines, need 150+)"}'
    </automated>
  </verify>
  <done>
    Complete design system documentation with responsive breakpoints, WCAG 2.1 AA accessibility standards, Button reference implementation, and quick reference guide. Documentation includes verification checklist, common patterns, do's and don'ts, and troubleshooting tips. All 6 foundation requirements (UI-FOUND-01 through UI-FOUND-06) fully documented.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Documentation → Developers | Documentation guides implementation but doesn't enforce constraints |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-04 | Information Disclosure | Design system documentation | accept | No risk: Documentation is internal development resource, no sensitive data, publicly accessible design patterns |
</threat_model>

<verification>
## Documentation Quality Checks

After task completion, verify:

1. **Completeness**: All 6 requirements (UI-FOUND-01 through UI-FOUND-06) documented
2. **Code examples**: At least 6 code blocks showing real implementation patterns
3. **Accessibility**: WCAG 2.1 AA standards explained with verification steps
4. **Usability**: Quick reference section allows developers to find common utilities fast
5. **Accuracy**: All color codes, spacing values, and timing values match src/index.css
6. **Clarity**: Non-technical stakeholders can understand color palette and typography sections
</verification>

<success_criteria>
1. DESIGN-SYSTEM.md exists with 150+ lines of comprehensive documentation
2. All neon colors documented with hex codes, RGB values, and Tailwind utilities
3. Typography system documented with font families, size scale, and weights
4. Spacing system documented with 8px grid and Tailwind utilities
5. Animation system documented with timing, easing, and reduced-motion support
6. Responsive breakpoints documented with mobile-first approach
7. WCAG 2.1 AA accessibility standards documented with verification checklist
8. Button component shown as reference implementation with complete code example
9. Quick reference section provides fast lookup for common utilities
10. Do's and don'ts section prevents common mistakes
11. Troubleshooting section helps debug common issues
</success_criteria>

<output>
Update `.planning/phases/01-design-system-foundation/01-01-SUMMARY.md` to include documentation completion.

Note in summary:
- Documentation sections completed (10 sections total)
- Code examples provided (count)
- Accessibility verification checklist included
- Quick reference and troubleshooting guides added
</output>
