---
phase: 05-semantic-discovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/schema.ts
  - electron/database/connection.ts
  - tests/embeddings.schema.test.ts
autonomous: true
requirements: [SEM-01, SEM-05]

must_haves:
  truths:
    - "System stores 384-dimensional embeddings for each note"
    - "Embeddings table has one-to-one relationship with notes table"
    - "Links table distinguishes manual wiki-links from semantic auto-links"
    - "Semantic links store similarity scores for ranking"
  artifacts:
    - path: "electron/database/schema.ts"
      provides: "embeddings table definition with vector blob column"
      contains: "export const embeddings = sqliteTable"
    - path: "electron/database/schema.ts"
      provides: "links table extended with link_type and similarity_score columns"
      contains: "link_type: text('link_type')"
    - path: "tests/embeddings.schema.test.ts"
      provides: "Schema validation tests"
      min_lines: 50
  key_links:
    - from: "embeddings.note_id"
      to: "notes.id"
      via: "foreign key CASCADE delete"
      pattern: "references\\(\\(\\) => notes\\.id.*onDelete.*cascade"
    - from: "links.link_type"
      to: "semantic link queries"
      via: "WHERE clause filtering"
      pattern: "eq\\(links\\.link_type.*'semantic'\\)"
---

<objective>
Extend database schema to support semantic discovery through embeddings storage and semantic link tracking.

Purpose: Enable vector storage for semantic search and distinguish between manual wiki-links and AI-discovered semantic relationships. This foundation allows the system to store 384-dimensional embeddings per note and track auto-discovered semantic links with similarity scores.

Output: Extended schema with embeddings table (note_id, vector blob, metadata) and links table extended with link_type ('manual'|'semantic') and similarity_score columns. All changes validated with comprehensive tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-semantic-discovery/05-CONTEXT.md
@.planning/phases/05-semantic-discovery/05-RESEARCH.md
@.planning/phases/05-semantic-discovery/05-PATTERNS.md
@electron/database/schema.ts
@electron/database/connection.ts
@tests/content.schema.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend database schema with embeddings table and links table modifications</name>
  <files>electron/database/schema.ts, electron/database/connection.ts, tests/embeddings.schema.test.ts</files>
  
  <read_first>
    - electron/database/schema.ts (current schema structure, existing tables pattern)
    - electron/database/connection.ts (table creation sequence, initialization pattern)
    - tests/content.schema.test.ts (schema test patterns, validation approach)
  </read_first>
  
  <behavior>
    - Test 1: embeddings table exists with 6 columns (id, note_id, vector, model, dimensions, created_at, updated_at)
    - Test 2: embeddings.note_id has UNIQUE constraint and CASCADE delete foreign key to notes.id
    - Test 3: embeddings.vector is blob type, embeddings.dimensions defaults to 384
    - Test 4: links table has link_type column with default 'manual'
    - Test 5: links table has similarity_score column (nullable real type)
    - Test 6: Can insert embedding with 384-dim vector blob and retrieve it
    - Test 7: Deleting note cascades to embeddings table
    - Test 8: Can create semantic link with similarity_score, manual link without score
  </behavior>
  
  <action>
Add embeddings table to electron/database/schema.ts following existing table patterns per D-04 and D-05:

**embeddings table structure:**
- id: text (primary key, UUID)
- note_id: text (foreign key to notes.id with CASCADE delete, UNIQUE constraint for one-to-one relationship)
- vector: blob with mode 'buffer' (stores 384 float32 values = 1536 bytes per D-05)
- model: text with default 'all-MiniLM-L6-v2' (tracks which model generated the embedding)
- dimensions: integer with default 384 (validates vector size per D-05)
- created_at: integer with mode 'timestamp'
- updated_at: integer with mode 'timestamp'

**Extend links table** (modify existing definition):
- Add link_type: text with default 'manual' (values: 'manual' for wiki-links, 'semantic' for auto-discovered per D-11)
- Add similarity_score: real nullable (stores cosine similarity 0.0-1.0 for semantic links only per D-09)

**Update electron/database/connection.ts:**
- Add embeddings table creation in initializeDatabase function after content table
- Follow existing pattern: db.run with CREATE TABLE IF NOT EXISTS using Drizzle schema

**Create tests/embeddings.schema.test.ts:**
- Import pattern from tests/content.schema.test.ts (Database, getDatabase, closeDatabase, beforeEach/afterEach setup)
- Test embeddings table structure (6 columns, correct types, UNIQUE note_id, CASCADE delete)
- Test links table extensions (link_type column exists, similarity_score column exists)
- Test embedding CRUD (insert with vector blob, retrieve, cascade delete)
- Test semantic vs manual link creation (semantic link has similarity_score, manual link does not)
- Use Buffer.from(new Float32Array(384).buffer) for test vector blobs
  </action>
  
  <verify>
    <automated>npm test -- embeddings.schema.test.ts --run</automated>
  </verify>
  
  <acceptance_criteria>
    - electron/database/schema.ts contains `export const embeddings = sqliteTable('embeddings'` with 7 columns
    - embeddings.note_id has `.unique()` and `.references(() => notes.id, { onDelete: 'cascade' })`
    - embeddings.vector uses `blob('vector', { mode: 'buffer' })`
    - embeddings.dimensions has `.default(384)`
    - links table definition includes `link_type: text('link_type').notNull().default('manual')`
    - links table definition includes `similarity_score: real('similarity_score')`
    - electron/database/connection.ts initializeDatabase function includes embeddings table creation
    - tests/embeddings.schema.test.ts exists with 8+ tests covering schema structure and CRUD operations
    - `npm test -- embeddings.schema.test.ts --run` exits 0
  </acceptance_criteria>
  
  <done>
Database schema extended with embeddings table (one-to-one with notes, CASCADE delete, 384-dim vector blob storage) and links table modified with link_type and similarity_score columns. All 8 schema tests passing, validating structure, constraints, and CRUD operations.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main Process → SQLite | Vector blobs and similarity scores written to database, must validate dimensions and score ranges |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | embeddings.vector blob | mitigate | Validate vector dimensions (must be 384) before INSERT, add CHECK constraint on dimensions column |
| T-05-02 | Denial of Service | embeddings table size | accept | 384 float32 = 1536 bytes per note, 1000 notes = 1.5MB, acceptable for desktop app |
| T-05-03 | Information Disclosure | similarity_score values | accept | Scores are 0.0-1.0 cosine similarity, no sensitive data, used for ranking only |
| T-05-04 | Tampering | link_type column | mitigate | Validate link_type is 'manual' or 'semantic' before INSERT, use CHECK constraint |
</threat_model>

<verification>
**Schema validation:**
- embeddings table has 7 columns with correct types and constraints
- note_id foreign key has CASCADE delete and UNIQUE constraint
- vector column is blob type with buffer mode
- dimensions column defaults to 384

**Links table extensions:**
- link_type column exists with default 'manual'
- similarity_score column exists as nullable real

**CRUD operations:**
- Can insert embedding with 384-dim vector blob
- Can retrieve embedding and vector blob matches inserted data
- Deleting note cascades to embeddings table
- Can create semantic link with similarity_score
- Can create manual link without similarity_score

**Test coverage:**
- All 8 tests in embeddings.schema.test.ts pass
- Tests cover schema structure, constraints, and CRUD operations
</verification>

<success_criteria>
- embeddings table exists in schema with 7 columns
- embeddings.note_id has UNIQUE constraint and CASCADE delete foreign key
- embeddings.vector is blob type for storing 384 float32 values
- links table has link_type column (default 'manual') and similarity_score column (nullable real)
- Database initialization creates embeddings table
- All schema tests pass (8+ tests)
- Requirements SEM-01 and SEM-05 foundation established
</success_criteria>

<output>
Create `.planning/phases/05-semantic-discovery/05-01-SUMMARY.md` when done
</output>
