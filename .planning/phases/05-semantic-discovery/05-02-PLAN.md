---
phase: 05-semantic-discovery
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
autonomous: false
requirements: [SEM-01]
user_setup:
  - service: "@xenova/transformers"
    why: "Local embeddings generation for semantic search"
    env_vars: []
    dashboard_config: []
  - service: "sqlite-vec"
    why: "Vector similarity search in SQLite"
    env_vars: []
    dashboard_config:
      - task: "Download platform-specific binary from GitHub releases"
        location: "https://github.com/asg017/sqlite-vec/releases"
        details: "Windows: vec0.dll, Linux: vec0.so, macOS: vec0.dylib"

must_haves:
  truths:
    - "@xenova/transformers package installed and available for import"
    - "sqlite-vec binary downloaded and ready for extension loading"
    - "Package legitimacy verified before installation"
  artifacts:
    - path: "package.json"
      provides: "@xenova/transformers dependency"
      contains: "@xenova/transformers"
    - path: "electron/extensions/vec0.dll"
      provides: "sqlite-vec extension binary (Windows)"
      min_lines: 0
  key_links:
    - from: "package.json"
      to: "node_modules/@xenova/transformers"
      via: "npm install"
      pattern: "@xenova/transformers.*2\\."
---

<objective>
Install and verify packages required for semantic discovery: @xenova/transformers for local embeddings generation and sqlite-vec extension for vector similarity search.

Purpose: Establish dependencies for generating 384-dimensional embeddings locally (no API costs, privacy-preserving per D-02) and performing vector similarity queries in SQLite (keeps everything in one database per D-04). Both packages flagged [ASSUMED] in research require human verification before installation.

Output: @xenova/transformers installed via npm, sqlite-vec binary downloaded and placed in electron/extensions/ directory, package legitimacy verified by human, ready for use in subsequent plans.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-semantic-discovery/05-CONTEXT.md
@.planning/phases/05-semantic-discovery/05-RESEARCH.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <what-built>
Package legitimacy verification for @xenova/transformers and sqlite-vec before installation.

**Research findings (from 05-RESEARCH.md Package Legitimacy Audit):**

1. **@xenova/transformers** - [ASSUMED]
   - Registry: npm
   - Age: 2 years (created 2023-03-03)
   - Source: github.com/xenova/transformers.js
   - Status: Package exists on npm, has GitHub repository
   - Risk: slopcheck unavailable, manual verification required

2. **sqlite-vec** - [ASSUMED]
   - Distribution: GitHub binary releases (not npm package)
   - Source: github.com/asg017/sqlite-vec
   - Status: Binary distribution from official GitHub releases
   - Risk: Not npm package, requires manual download verification

**Why verification needed:**
Per Package Legitimacy Gate protocol, [ASSUMED] packages require human verification before installation. Both packages are legitimate but could not be automatically verified via slopcheck.
  </what-built>
  
  <how-to-verify>
**Step 1: Verify @xenova/transformers on npm**
1. Visit https://www.npmjs.com/package/@xenova/transformers
2. Check package details:
   - Published by: xenova
   - Repository: github.com/xenova/transformers.js
   - Weekly downloads: Should be 10K+
   - Last publish: Within last 6 months
3. Visit GitHub repository: https://github.com/xenova/transformers.js
4. Verify: 1K+ stars, active maintenance, official Hugging Face project

**Step 2: Verify sqlite-vec on GitHub**
1. Visit https://github.com/asg017/sqlite-vec
2. Check repository details:
   - Author: asg017 (Alex Garcia, known SQLite extension developer)
   - Stars: Should be 500+
   - Recent releases: Within last 6 months
3. Check releases page: https://github.com/asg017/sqlite-vec/releases
4. Verify: Platform-specific binaries available (vec0.dll, vec0.so, vec0.dylib)

**Expected outcome:**
- @xenova/transformers is legitimate npm package from Hugging Face ecosystem
- sqlite-vec is legitimate SQLite extension from trusted developer
- Both packages safe to install
  </how-to-verify>
  
  <resume-signal>
Type "approved" to proceed with installation, or describe concerns if packages appear suspicious.
  </resume-signal>
</task>

<task type="auto">
  <name>Task 1: Install @xenova/transformers via npm</name>
  <files>package.json</files>
  
  <read_first>
    - package.json (current dependencies, version patterns)
  </read_first>
  
  <action>
Install @xenova/transformers package per D-02 (local embeddings generation):

Run: `npm install @xenova/transformers`

This installs the latest stable version (2.17.2 or newer) which provides:
- pipeline('feature-extraction') for generating embeddings
- Support for all-MiniLM-L6-v2 model (384 dimensions per D-05)
- ONNX runtime for running transformer models in Node.js
- Built-in model caching to user data directory

Package will be added to package.json dependencies section.
  </action>
  
  <verify>
    <automated>npm list @xenova/transformers</automated>
  </verify>
  
  <acceptance_criteria>
    - package.json contains "@xenova/transformers" in dependencies section
    - `npm list @xenova/transformers` shows installed version (2.x or higher)
    - node_modules/@xenova/transformers directory exists
  </acceptance_criteria>
  
  <done>
@xenova/transformers installed successfully, available for import in embeddings service. Package provides local embeddings generation with all-MiniLM-L6-v2 model support.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <action-required>
Download sqlite-vec extension binary for your platform and place in electron/extensions/ directory.

**Why manual:** sqlite-vec is distributed as platform-specific binaries via GitHub releases, not as npm package. No CLI/API available for automated download.

**Steps:**
1. Visit https://github.com/asg017/sqlite-vec/releases
2. Download latest release for your platform:
   - **Windows:** vec0.dll
   - **Linux:** vec0.so
   - **macOS:** vec0.dylib
3. Create directory: `electron/extensions/`
4. Place downloaded binary in `electron/extensions/vec0.dll` (or .so/.dylib)
5. Verify file size: Should be 500KB-2MB

**Current platform:** Windows (per env context)
**Required file:** electron/extensions/vec0.dll
  </action-required>
  
  <verification-steps>
After downloading and placing the binary:
1. Check file exists: `ls electron/extensions/vec0.dll`
2. Check file size: `ls -lh electron/extensions/vec0.dll` (should be ~1MB)
3. Verify it's a DLL: `file electron/extensions/vec0.dll` (should show "PE32+ executable")
  </verification-steps>
  
  <resume-signal>
Type "done" when binary is downloaded and placed in electron/extensions/ directory.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local | @xenova/transformers package downloaded from npm, must verify legitimacy |
| GitHub releases → local | sqlite-vec binary downloaded from GitHub, must verify source and integrity |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering | npm/GitHub installs | mitigate | Human verification checkpoint for [ASSUMED] packages before installation |
| T-05-05 | Tampering | @xenova/transformers package | mitigate | Verify package on npmjs.com (publisher, downloads, repository), check GitHub stars/activity |
| T-05-06 | Tampering | sqlite-vec binary | mitigate | Verify GitHub repository (author asg017, stars, recent releases), download from official releases only |
| T-05-07 | Denial of Service | Model download (80MB) | accept | First-time model download may take 30-60 seconds, acceptable for desktop app |
</threat_model>

<verification>
**Package installation:**
- @xenova/transformers appears in package.json dependencies
- npm list shows installed version 2.x or higher
- node_modules/@xenova/transformers directory exists

**Binary placement:**
- electron/extensions/vec0.dll exists (Windows) or .so (Linux) or .dylib (macOS)
- File size is reasonable (500KB-2MB range)
- File is correct type (DLL/shared library)

**Legitimacy verification:**
- Human verified @xenova/transformers on npmjs.com and GitHub
- Human verified sqlite-vec on GitHub releases
- Both packages approved for installation
</verification>

<success_criteria>
- @xenova/transformers installed via npm and available for import
- sqlite-vec binary downloaded and placed in electron/extensions/
- Package legitimacy verified by human before installation
- Both dependencies ready for use in embeddings service and vector search
- Requirement SEM-01 dependencies established
</success_criteria>

<output>
Create `.planning/phases/05-semantic-discovery/05-02-SUMMARY.md` when done
</output>
