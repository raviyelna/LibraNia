---
phase: 02-frontend-adaptation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/api/client.ts
  - src/api/notes.ts
  - src/api/content.ts
  - src/api/chat.ts
  - src/api/search.ts
  - src/api/tags.ts
  - src/api/config.ts
  - src/api/ai.ts
  - src/api/index.ts
  - src/utils/toast.ts
  - package.json
autonomous: true
requirements: [FRONT-02]

must_haves:
  truths:
    - "API client can make HTTP requests to backend endpoints"
    - "API errors are caught and categorized (network vs HTTP)"
    - "Toast notifications show user-friendly error messages"
    - "All domain APIs (notes, content, chat, etc.) are callable from frontend"
  artifacts:
    - path: "src/api/client.ts"
      provides: "Base fetch wrapper with error handling"
      exports: ["apiRequest", "APIError"]
      min_lines: 40
    - path: "src/api/notes.ts"
      provides: "Notes API methods"
      exports: ["notesAPI"]
      min_lines: 30
    - path: "src/api/content.ts"
      provides: "Content API methods with upload support"
      exports: ["contentAPI"]
      min_lines: 40
    - path: "src/utils/toast.ts"
      provides: "Toast notification helpers"
      exports: ["handleAPIError"]
      min_lines: 20
  key_links:
    - from: "src/api/notes.ts"
      to: "src/api/client.ts"
      via: "import apiRequest"
      pattern: "import.*apiRequest.*from.*client"
    - from: "src/utils/toast.ts"
      to: "src/api/client.ts"
      via: "import APIError"
      pattern: "import.*APIError.*from.*api/client"
---

<objective>
Create centralized HTTP API client infrastructure with domain-specific modules (notes, content, chat, search, tags, config, ai) and error handling with toast notifications.

Purpose: Establish foundation for replacing Electron IPC calls with standard web fetch API. Centralized client enables consistent error handling, base URL configuration, and retry logic.

Output: API client modules ready for hook integration in Wave 2. All backend endpoints accessible via typed TypeScript functions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-adaptation/02-CONTEXT.md
@.planning/phases/02-frontend-adaptation/02-RESEARCH.md
@.planning/phases/01-backend-extraction/01-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install react-hot-toast dependency</name>
  <files>package.json</files>
  <action>
Install react-hot-toast@2.4.1 for toast notifications per D-13, D-14, D-15. Run npm install react-hot-toast@2.4.1. Verify installation with npm list react-hot-toast.
  </action>
  <verify>
    <automated>npm list react-hot-toast | grep -q "react-hot-toast@2.4.1"</automated>
  </verify>
  <done>react-hot-toast@2.4.1 appears in package.json dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create base API client with error handling</name>
  <files>src/api/client.ts, src/api/client.test.ts</files>
  <behavior>
    - Test 1: apiRequest returns parsed JSON for successful 200 response
    - Test 2: apiRequest throws APIError with status 404 for not found
    - Test 3: apiRequest throws APIError with status 0 for network errors
    - Test 4: APIError includes status code and error message
    - Test 5: Base URL uses VITE_API_URL env var with localhost:3000 fallback
  </behavior>
  <action>
Create src/api/client.ts with APIError class (extends Error, adds status and data fields) and apiRequest function. Per D-03, use VITE_API_URL env var with fallback to http://localhost:3000. Implement fetch wrapper that checks response.ok (fetch only rejects on network errors, not HTTP errors per Pitfall 1 in RESEARCH.md). For HTTP errors (response.ok === false), parse error JSON and throw APIError with status code. For network errors (fetch rejection), throw APIError with status 0. Set Content-Type: application/json header by default. Return parsed JSON response body. Create test file with 5 behavior tests covering success, 404, network error, error structure, and base URL.
  </action>
  <verify>
    <automated>npm test -- src/api/client.test.ts --run</automated>
  </verify>
  <done>apiRequest function handles success, HTTP errors, and network errors correctly. All 5 tests pass.</done>
</task>

<task type="auto">
  <name>Task 3: Create domain API modules (notes, content, chat, search, tags, config, ai)</name>
  <files>src/api/notes.ts, src/api/content.ts, src/api/chat.ts, src/api/search.ts, src/api/tags.ts, src/api/config.ts, src/api/ai.ts, src/api/index.ts</files>
  <action>
Create 7 domain API modules per D-01, D-02. Each module imports apiRequest from ./client and exports an object with methods matching backend routes from 01-03-SUMMARY.md.

**notes.ts:** Export notesAPI with methods: getAll() → GET /api/notes, getById(id) → GET /api/notes/:id, create(data) → POST /api/notes, update(id, data) → PUT /api/notes/:id, delete(id) → DELETE /api/notes/:id, restore(id) → POST /api/notes/:id/restore, getDeleted() → GET /api/notes/deleted.

**content.ts:** Export contentAPI with methods: getAll() → GET /api/content, getById(id) → GET /api/content/:id, delete(id) → DELETE /api/content/:id. Note: upload method will be added in Plan 03 (requires FormData and XMLHttpRequest for progress tracking per D-10, D-11).

**chat.ts:** Export chatAPI with methods: summarizeNote(noteId) → POST /api/ai/summarize. Note: streaming chat methods will use Socket.IO (Plan 02), not HTTP.

**search.ts:** Export searchAPI with methods: search(query, type) → POST /api/search, semantic(query) → POST /api/search/semantic.

**tags.ts:** Export tagsAPI with methods: getAll() → GET /api/tags, create(data) → POST /api/tags, update(id, data) → PUT /api/tags/:id, delete(id) → DELETE /api/tags/:id, getByNote(noteId) → GET /api/tags/note/:noteId, addToNote(noteId, tagId) → POST /api/tags/note/:noteId, removeFromNote(noteId, tagId) → DELETE /api/tags/note/:noteId/:tagId.

**config.ts:** Export configAPI with methods: get() → GET /api/config, update(data) → PUT /api/config, reset() → POST /api/config/reset.

**ai.ts:** Export aiAPI with methods: getModels() → GET /api/ai/models, getProviders() → GET /api/ai/providers, validateKey(providerId, apiKey) → POST /api/ai/validate-key.

**index.ts:** Re-export all API modules for convenient imports: export * from './notes'; export * from './content'; etc.

All methods use apiRequest with appropriate HTTP method and body. Return unwrapped data (e.g., response.data or response.note) not full { success, data } wrapper.
  </action>
  <verify>
    <automated>grep -c "export const.*API" src/api/*.ts | grep -v "^0$" | wc -l | grep -q "7"</automated>
  </verify>
  <done>7 domain API modules created with methods matching backend routes. index.ts re-exports all modules.</done>
</task>

<task type="auto">
  <name>Task 4: Create toast notification helper</name>
  <files>src/utils/toast.ts</files>
  <action>
Create src/utils/toast.ts with handleAPIError function per D-13, D-14, D-15. Import toast from react-hot-toast and APIError from ../api/client. Function accepts error: unknown parameter. If error instanceof APIError: check status code - if status === 0 (network error), show toast.error('Connection failed - check your network'); if status >= 400 and < 500 (client error), show toast.error(error.message); else (server error), show toast.error('Server error - please try again'). For non-APIError, show toast.error('An unexpected error occurred'). Always console.error the full error for debugging per D-16. Export handleAPIError function.
  </action>
  <verify>
    <automated>grep -q "export function handleAPIError" src/utils/toast.ts && grep -q "import.*toast.*from.*react-hot-toast" src/utils/toast.ts</automated>
  </verify>
  <done>handleAPIError function categorizes errors by type and shows appropriate toast messages</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Backend API | Untrusted frontend makes HTTP requests to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | API client base URL | accept | Base URL from env var, no secrets in frontend code |
| T-02-02 | Tampering | HTTP requests | mitigate | Backend validates all inputs (Phase 1), frontend only sends data |
| T-02-03 | Denial of Service | API error loops | mitigate | Toast notifications prevent error spam, console.error for debugging |
| T-02-SC | Tampering | npm installs | mitigate | react-hot-toast verified [OK] in RESEARCH.md Package Legitimacy Audit |
</threat_model>

<verification>
## Plan-Level Checks

```bash
# All API modules created
test -f src/api/client.ts && test -f src/api/notes.ts && test -f src/api/content.ts && test -f src/api/chat.ts && test -f src/api/search.ts && test -f src/api/tags.ts && test -f src/api/config.ts && test -f src/api/ai.ts && test -f src/api/index.ts

# Toast helper created
test -f src/utils/toast.ts

# Tests pass
npm test -- src/api/client.test.ts --run

# TypeScript compiles
npm run build
```
</verification>

<success_criteria>
- react-hot-toast installed and listed in package.json
- Base API client (client.ts) handles success, HTTP errors, and network errors
- 7 domain API modules created with methods matching backend routes
- Toast notification helper categorizes errors and shows user-friendly messages
- All TypeScript compiles without errors
- Client tests pass (5 behavior tests)
</success_criteria>

<output>
Create `.planning/phases/02-frontend-adaptation/02-01-SUMMARY.md` when done
</output>
