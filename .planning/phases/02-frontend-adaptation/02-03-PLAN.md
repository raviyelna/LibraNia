---
phase: 02-frontend-adaptation
plan: 03
type: execute
wave: 2
depends_on: [02-01, 02-02]
files_modified:
  - src/hooks/useNotes.ts
  - src/hooks/useContent.ts
  - src/hooks/useChat.ts
  - src/hooks/useSearch.ts
  - src/hooks/useTags.ts
  - src/hooks/useAIProviders.ts
  - src/hooks/useConversations.ts
  - src/components/ContentUpload.tsx
  - src/api/content.ts
autonomous: true
requirements: [FRONT-01, FRONT-02, FRONT-03, FRONT-05]

must_haves:
  truths:
    - "All window.api.* calls replaced with HTTP or Socket.IO"
    - "Notes CRUD operations work via HTTP API"
    - "File uploads work via HTML input with progress tracking"
    - "AI chat streaming works via Socket.IO"
    - "Real-time note updates work via Socket.IO events"
    - "Search, tags, config operations work via HTTP API"
  artifacts:
    - path: "src/hooks/useNotes.ts"
      provides: "Notes hooks using HTTP API"
      contains: "notesAPI"
    - path: "src/hooks/useChat.ts"
      provides: "Chat hooks using Socket.IO"
      contains: "useSocket"
    - path: "src/components/ContentUpload.tsx"
      provides: "File upload with HTML input and progress"
      contains: "<input type=\"file\""
    - path: "src/api/content.ts"
      provides: "Content upload with XMLHttpRequest progress"
      exports: ["uploadContent"]
  key_links:
    - from: "src/hooks/useNotes.ts"
      to: "src/api/notes.ts"
      via: "import notesAPI"
      pattern: "import.*notesAPI.*from.*api"
    - from: "src/hooks/useChat.ts"
      to: "src/contexts/SocketContext.tsx"
      via: "import useSocket"
      pattern: "import.*useSocket.*from.*contexts"
    - from: "src/components/ContentUpload.tsx"
      to: "src/api/content.ts"
      via: "import uploadContent"
      pattern: "import.*uploadContent.*from.*api"
---

<objective>
Replace all Electron IPC calls (window.api.*) with HTTP API client or Socket.IO. Migrate 12 hooks and file upload component to use web-standard APIs.

Purpose: Complete frontend migration from Electron to web. After this plan, frontend has zero Electron dependencies and works as standard React web app.

Output: All 33 window.api.* call sites replaced. Notes, content, search, tags, config use HTTP. Chat streaming and real-time updates use Socket.IO. File uploads use HTML input with progress tracking.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-frontend-adaptation/02-CONTEXT.md
@.planning/phases/02-frontend-adaptation/02-RESEARCH.md
@.planning/phases/02-frontend-adaptation/02-01-SUMMARY.md
@.planning/phases/02-frontend-adaptation/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate useNotes hook to HTTP API</name>
  <files>src/hooks/useNotes.ts</files>
  <action>
Read src/hooks/useNotes.ts. Replace all window.api.notes.* calls with notesAPI.* from ../api/notes per D-04. Import notesAPI from '../api'. Import handleAPIError from '../utils/toast'. Import useSocket from '../contexts/SocketContext'.

Replace window.api.notes.getAll() with notesAPI.getAll(). Replace window.api.notes.getById() with notesAPI.getById(). Replace window.api.notes.create() with notesAPI.create(). Replace window.api.notes.update() with notesAPI.update(). Replace window.api.notes.delete() with notesAPI.delete(). Add try-catch blocks around API calls and call handleAPIError(error) in catch blocks per D-13, D-14.

For real-time updates (window.api.notes.onUpdated), replace with Socket.IO per D-07 and RESEARCH.md Pattern 2. In useNote hook, get { socket } from useSocket(). In useEffect, check if (!socket) return. Register socket.on('note:updated', handleNoteUpdated) where handleNoteUpdated checks if updatedNote.id === id and calls setNote(updatedNote). Return cleanup function that calls socket.off('note:updated', handleNoteUpdated) per Pitfall 3 in RESEARCH.md. Dependencies: [socket, id].

Remove all window.api references. Preserve all existing hook logic, state management, and return values.
  </action>
  <verify>
    <automated>grep -c "window\.api" src/hooks/useNotes.ts | grep -q "^0$" && grep -q "notesAPI" src/hooks/useNotes.ts && grep -q "useSocket" src/hooks/useNotes.ts</automated>
  </verify>
  <done>useNotes hook uses HTTP API for CRUD, Socket.IO for real-time updates, no window.api calls remain</done>
</task>

<task type="auto">
  <name>Task 2: Add file upload method to content API and migrate useContent hook</name>
  <files>src/api/content.ts, src/hooks/useContent.ts</files>
  <action>
First, add upload method to src/api/content.ts per D-09, D-10, D-11 and RESEARCH.md Pattern 3. Export uploadContent function accepting (file: File, source: 'manual' | 'ai-generated', onProgress?: (percent: number) => void). Use XMLHttpRequest (not fetch) for upload progress tracking per Pitfall 5 in RESEARCH.md. Create FormData, append file and source. Get apiUrl from import.meta.env.VITE_API_URL with fallback. Create XMLHttpRequest instance. If onProgress provided, register xhr.upload.addEventListener('progress', (e) => { if (e.lengthComputable) onProgress((e.loaded / e.total) * 100) }). Register xhr.addEventListener('load') to resolve promise with parsed response.content on 200, reject with error message otherwise. Register xhr.addEventListener('error') to reject with 'Network error during upload'. Call xhr.open('POST', `${apiUrl}/api/content/upload`) and xhr.send(formData). Return Promise<Content>.

Then read src/hooks/useContent.ts. Replace all window.api.content.* calls with contentAPI.* from ../api. Import contentAPI and uploadContent from '../api'. Import handleAPIError from '../utils/toast'. Replace window.api.content.getAll() with contentAPI.getAll(). Replace window.api.content.getById() with contentAPI.getById(). Replace window.api.content.delete() with contentAPI.delete(). For upload, replace window.api.content.upload() with uploadContent(file, source, (percent) => setProgress(percent)). Add try-catch with handleAPIError. Remove all window.api references.
  </action>
  <verify>
    <automated>grep -c "window\.api" src/hooks/useContent.ts | grep -q "^0$" && grep -q "uploadContent" src/api/content.ts && grep -q "XMLHttpRequest" src/api/content.ts</automated>
  </verify>
  <done>uploadContent function uses XMLHttpRequest for progress, useContent hook uses HTTP API, no window.api calls remain</done>
</task>

<task type="auto">
  <name>Task 3: Migrate useChat hook to Socket.IO</name>
  <files>src/hooks/useChat.ts</files>
  <action>
Read src/hooks/useChat.ts. Replace window.api.chat.* calls with Socket.IO per D-05, D-06, D-07. Import useSocket from '../contexts/SocketContext'. Import handleAPIError from '../utils/toast'. Import chatAPI from '../api' (for non-streaming operations like summarizeNote).

In useSendMessage hook, get { socket } from useSocket(). Replace window.api.chat.onToken listener with socket.on('ai:token', handleToken) in useEffect per RESEARCH.md Pattern 2. handleToken checks if data.conversationId matches and appends data.token to streamingContent. Return cleanup function that calls socket.off('ai:token', handleToken). Dependencies: [socket, conversationId].

Replace window.api.chat.send() with socket.emit('ai:chat', { conversationId, messages: [{ role: 'user', content: message }], providerId, model }). Remove the cleanup() call from try block since cleanup is now in useEffect.

In useSummarizeNote hook, replace window.api.chat.summarizeNote() with chatAPI.summarizeNote(noteId). Add try-catch with handleAPIError.

Remove all window.api references.
  </action>
  <verify>
    <automated>grep -c "window\.api" src/hooks/useChat.ts | grep -q "^0$" && grep -q "useSocket" src/hooks/useChat.ts && grep -q "socket.emit" src/hooks/useChat.ts</automated>
  </verify>
  <done>useChat hook uses Socket.IO for streaming, HTTP API for summarize, no window.api calls remain</done>
</task>

<task type="auto">
  <name>Task 4: Migrate useSearch, useTags, useAIProviders, useConversations hooks to HTTP API</name>
  <files>src/hooks/useSearch.ts, src/hooks/useTags.ts, src/hooks/useAIProviders.ts, src/hooks/useConversations.ts</files>
  <action>
Read each hook file and replace window.api.* calls with corresponding API client methods per D-04.

**useSearch.ts:** Import searchAPI from '../api'. Import handleAPIError from '../utils/toast'. Replace window.api.search.search() with searchAPI.search(query, type). Replace window.api.search.semantic() with searchAPI.semantic(query). Add try-catch with handleAPIError. Remove all window.api references.

**useTags.ts:** Import tagsAPI from '../api'. Import handleAPIError from '../utils/toast'. Replace window.api.tags.getAll() with tagsAPI.getAll(). Replace window.api.tags.create() with tagsAPI.create(). Replace window.api.tags.update() with tagsAPI.update(). Replace window.api.tags.delete() with tagsAPI.delete(). Replace window.api.tags.getByNote() with tagsAPI.getByNote(). Replace window.api.tags.addToNote() with tagsAPI.addToNote(). Replace window.api.tags.removeFromNote() with tagsAPI.removeFromNote(). Add try-catch with handleAPIError. Remove all window.api references.

**useAIProviders.ts:** Import aiAPI from '../api'. Import handleAPIError from '../utils/toast'. Replace window.api.ai.getProviders() with aiAPI.getProviders(). Replace window.api.ai.getModels() with aiAPI.getModels(). Replace window.api.ai.validateKey() with aiAPI.validateKey(). Add try-catch with handleAPIError. Remove all window.api references.

**useConversations.ts:** Import conversationsAPI from '../api' (if exists, otherwise note that conversations routes may need to be added to API client based on backend routes). Replace window.api.conversations.* calls with conversationsAPI.* calls. Add try-catch with handleAPIError. Remove all window.api references. If conversationsAPI doesn't exist in src/api/, create src/api/conversations.ts with methods matching backend conversation routes.

Preserve all existing hook logic, state management, and return values in all hooks.
  </action>
  <verify>
    <automated>grep -c "window\.api" src/hooks/useSearch.ts src/hooks/useTags.ts src/hooks/useAIProviders.ts src/hooks/useConversations.ts | grep -v "^0$" | wc -l | grep -q "^0$"</automated>
  </verify>
  <done>All 4 hooks use HTTP API, no window.api calls remain in any hook file</done>
</task>

<task type="auto">
  <name>Task 5: Update ContentUpload component to use HTML file input</name>
  <files>src/components/ContentUpload.tsx</files>
  <action>
Read src/components/ContentUpload.tsx. Replace Electron dialog API with HTML file input per D-09, D-10, D-11 and RESEARCH.md Pattern 3. Import useRef, useState from react. Import uploadContent from '../api/content'. Import toast from 'react-hot-toast'.

Create fileInputRef with useRef<HTMLInputElement>(null). Create dragActive state with useState(false). Create uploading state with useState(false). Create progress state with useState(0).

Create handleFile async function accepting file: File. Set uploading to true, progress to 0. Try uploadContent(file, 'manual', (percent) => setProgress(percent)). On success, show toast.success(`Uploaded ${file.name}`) and call onUploadComplete callback if provided. On error, show toast.error(error.message). Finally set uploading to false.

Create handleDrop function for drag-drop: e.preventDefault(), setDragActive(false), get file from e.dataTransfer.files[0], if file exists call handleFile(file).

Return div with drag-drop zone per D-09. Add onDragOver handler that calls e.preventDefault() and setDragActive(true). Add onDragLeave handler that calls setDragActive(false). Add onDrop handler that calls handleDrop. Add onClick handler that calls fileInputRef.current?.click().

Inside div, render hidden input with ref={fileInputRef}, type="file", onChange handler that gets file from e.target.files?.[0] and calls handleFile if file exists. Render upload icon and text showing upload status: if uploading show "Uploading... {progress.toFixed(0)}%", else show "Click or drag file to upload". If uploading, render progress element with value={progress} max={100}.

Remove all window.api references and Electron dialog calls.
  </action>
  <verify>
    <automated>grep -c "window\.api" src/components/ContentUpload.tsx | grep -q "^0$" && grep -q "type=\"file\"" src/components/ContentUpload.tsx && grep -q "uploadContent" src/components/ContentUpload.tsx</automated>
  </verify>
  <done>ContentUpload uses HTML file input with drag-drop and progress tracking, no window.api calls remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Backend API | Frontend hooks make HTTP/WebSocket requests |
| User file system → Browser | File upload via HTML input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Tampering | File uploads | mitigate | Backend validates file types and size (Phase 1), frontend only sends files |
| T-02-08 | Information Disclosure | API calls | mitigate | Error messages sanitized via handleAPIError, no sensitive data in errors |
| T-02-09 | Denial of Service | Socket listeners | mitigate | Cleanup functions remove listeners on unmount (prevents memory leaks) |
| T-02-10 | Tampering | Real-time updates | mitigate | Backend validates all Socket.IO messages (Phase 1), frontend only receives data |
</threat_model>

<verification>
## Plan-Level Checks

```bash
# No window.api calls remain in hooks
grep -r "window\.api" src/hooks --include="*.ts" --include="*.tsx" | wc -l | grep -q "^0$"

# No window.api calls in ContentUpload
grep -c "window\.api" src/components/ContentUpload.tsx | grep -q "^0$"

# File upload uses HTML input
grep -q "type=\"file\"" src/components/ContentUpload.tsx

# Hooks use API client
grep -q "notesAPI" src/hooks/useNotes.ts
grep -q "useSocket" src/hooks/useChat.ts

# TypeScript compiles
npm run build
```
</verification>

<success_criteria>
- All 33 window.api.* call sites replaced (0 remaining in hooks and components)
- useNotes, useContent, useSearch, useTags, useAIProviders, useConversations use HTTP API
- useChat uses Socket.IO for streaming
- Real-time note updates use Socket.IO events
- ContentUpload uses HTML file input with drag-drop and progress
- uploadContent function uses XMLHttpRequest for progress tracking
- All error handling uses handleAPIError with toast notifications
- All TypeScript compiles without errors
- FRONT-01, FRONT-02, FRONT-03 requirements satisfied
</success_criteria>

<output>
Create `.planning/phases/02-frontend-adaptation/02-03-SUMMARY.md` when done
</output>
