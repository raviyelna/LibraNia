---
phase: 06-3d-visualization
plan: 04
type: execute
wave: 4
depends_on: [06-03]
files_modified:
  - src/components/Graph/GraphView.tsx
  - src/components/Graph/GraphControls.tsx
  - src/App.tsx
autonomous: true
requirements: [VIZ-02, VIZ-03, VIZ-04]
user_setup: []

must_haves:
  truths:
    - "Graph uses instanced rendering for 1000+ node performance"
    - "Force simulation tuned per user decisions (moderate clustering, strong center gravity, short links)"
    - "Clicking node highlights node + direct neighbors"
    - "Graph accessible via navigation menu"
    - "Node colors determined by tag (consistent hash-based colors)"
  artifacts:
    - path: "src/components/Graph/GraphView.tsx"
      provides: "Optimized 3D graph with instanced rendering and force tuning"
      contains: "InstancedMesh"
      min_lines: 150
    - path: "src/components/Graph/GraphControls.tsx"
      provides: "Graph control UI (future: search integration, filters)"
      exports: ["GraphControls"]
      min_lines: 20
  key_links:
    - from: "src/components/Graph/GraphView.tsx"
      to: "three"
      via: "InstancedMesh for node rendering"
      pattern: "InstancedMesh"
    - from: "src/components/Graph/GraphView.tsx"
      to: "d3-force-3d"
      via: "Force simulation configuration"
      pattern: "d3Force"
---

<objective>
Optimize graph rendering with instanced rendering, tune force simulation, add neighbor highlighting, and integrate into navigation.

Purpose: Achieve 1000+ node performance target (VIZ-02), implement user-decided graph layout and interaction patterns, make graph accessible from main navigation.
Output: Instanced rendering for nodes, force simulation tuned per D-01 through D-04, neighbor highlighting per D-14, navigation link to /graph.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-3d-visualization/06-CONTEXT.md
@.planning/phases/06-3d-visualization/06-RESEARCH.md
@src/components/Graph/GraphView.tsx
@src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Instanced Rendering for Performance</name>
  <files>src/components/Graph/GraphView.tsx</files>
  <behavior>
    - Nodes rendered using Three.js InstancedMesh (single draw call)
    - Node color determined by tag (hash-based consistent colors)
    - Untagged nodes get default gray color
    - Sphere geometry with low poly count (8 segments) for performance
    - MeshLambertMaterial for simple lighting
  </behavior>
  <action>
Update GraphView.tsx to use instanced rendering per RESEARCH.md Pattern 2:
- Import * as THREE from 'three'
- Create shared geometry and material outside component or in useMemo:
  - geometry = new THREE.SphereGeometry(5, 8, 8) // Low poly for performance per RESEARCH.md Pitfall 6
  - material = new THREE.MeshLambertMaterial() // Simple material for performance
- Add nodeThreeObject prop to ForceGraph3D:
  - Return InstancedMesh for each node
  - Set color based on node.tags[0] using getColorForTag helper
  - Use mesh.setColorAt(0, new THREE.Color(color))
  - Set mesh.instanceColor.needsUpdate = true
- Add nodeThreeObjectExtend={false} to prevent default node rendering
- Implement getColorForTag(tag: string): string helper:
  - Hash tag name to consistent color (same tag always same color)
  - Use color palette: ['#3b82f6', '#ef4444', '#10b981', '#f59e0b', '#8b5cf6', '#ec4899']
  - Return '#888888' for untagged nodes
  - Hash algorithm: tag.split('').reduce((acc, char) => acc + char.charCodeAt(0), 0) % colors.length

Follow RESEARCH.md Pattern 2 exactly. This optimization reduces 1000 draw calls to 1, critical for VIZ-02 requirement.
  </action>
  <verify>
    <automated>grep -c "InstancedMesh" src/components/Graph/GraphView.tsx</automated>
  </verify>
  <done>
Instanced rendering implemented, nodes rendered with single draw call, colors determined by tag hash. Performance optimized for 1000+ nodes per VIZ-02.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Tune Force Simulation Per User Decisions</name>
  <files>src/components/Graph/GraphView.tsx</files>
  <behavior>
    - Charge force: -40 (moderate repulsion per D-01, D-04)
    - Link distance: 40 (short links per D-03)
    - Link strength: 1 (strong connections)
    - Center force: 0.8 (strong center gravity per D-02)
    - Collision force: radius 10 (prevent overlap per D-04)
  </behavior>
  <action>
Add force simulation configuration per RESEARCH.md Pattern 3:
- Create ref for ForceGraph3D: const fgRef = useRef()
- Add ref prop to ForceGraph3D: ref={fgRef}
- Add useEffect to configure forces after mount:
  ```typescript
  useEffect(() => {
    const fg = fgRef.current;
    if (!fg) return;
    
    // Per D-01, D-04: Moderate repulsion
    fg.d3Force('charge').strength(-40);
    
    // Per D-03: Short link distance
    fg.d3Force('link').distance(40).strength(1);
    
    // Per D-02: Strong center gravity
    fg.d3Force('center').strength(0.8);
    
    // Per D-04: Prevent overlap
    fg.d3Force('collision', d3.forceCollide(10));
  }, []);
  ```
- Import d3-force-3d if needed for forceCollide

Values from RESEARCH.md Pattern 3 and user decisions D-01 through D-04. These create moderate clustering with strong center gravity and short links.
  </action>
  <verify>
    <automated>grep -c "d3Force" src/components/Graph/GraphView.tsx</automated>
  </verify>
  <done>
Force simulation tuned per user decisions: moderate clustering (-40 charge), strong center gravity (0.8), short links (40 distance), collision prevention. Graph layout matches D-01 through D-04.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement Neighbor Highlighting on Node Click</name>
  <files>src/components/Graph/GraphView.tsx</files>
  <behavior>
    - Clicking node highlights clicked node + direct neighbors (per D-14)
    - Highlighted nodes: white color
    - Non-highlighted nodes: dimmed (dark gray)
    - Highlighted links: white, width 2
    - Non-highlighted links: dark gray, width 1
    - Clicking background clears highlighting
  </behavior>
  <action>
Add neighbor highlighting per RESEARCH.md Pattern 4:
- Add state: highlightNodes (Set), highlightLinks (Set)
- Update handleNodeClick callback:
  - Find all neighbors of clicked node (iterate graphData.links)
  - If link.source === node.id or link.target === node.id, add neighbor to Set
  - Add clicked node to Set
  - Collect matching links in Set
  - setHighlightNodes(neighbors), setHighlightLinks(links)
  - Keep existing setSelectedNoteId(node.id) for side panel
- Add nodeColor prop to ForceGraph3D:
  - If highlightNodes.size > 0: return highlightNodes.has(node.id) ? '#ffffff' : '#444444'
  - Else: return getColorForTags(node.tags) (existing color logic)
- Add linkColor prop: highlightLinks.has(link) ? '#ffffff' : '#444444'
- Add linkWidth prop: highlightLinks.has(link) ? 2 : 1
- Update GraphSidePanel onClose to also clear highlighting:
  - onClose={() => { setSelectedNoteId(null); setHighlightNodes(new Set()); setHighlightLinks(new Set()); }}

Handle both link.source as string and as object (link.source.id) for compatibility with d3-force-3d internal representation.
  </action>
  <verify>
    <automated>npm test -- GraphView.test.ts --run</automated>
  </verify>
  <done>
Neighbor highlighting implemented per D-14: clicked node + direct neighbors highlighted in white, others dimmed, links highlighted. Clicking background or closing panel clears highlighting.
  </done>
</task>

<task type="auto">
  <name>Task 4: Create GraphControls Component (Placeholder)</name>
  <files>src/components/Graph/GraphControls.tsx</files>
  <action>
Create placeholder GraphControls component for future features (search integration, filters):
- Export function GraphControls()
- Return simple div with "Graph Controls" text
- Styled with Tailwind: absolute top-4 left-4 bg-background p-4 rounded shadow
- Comment: "TODO: Add search integration (D-11), filters, minimap (D-12)"

This component is a placeholder for Wave 5 features. Creating now establishes structure and integration point.
  </action>
  <verify>
    <automated>test -f src/components/Graph/GraphControls.tsx && echo "exists"</automated>
  </verify>
  <done>
GraphControls placeholder component created, positioned in graph view, ready for future features (search, filters, minimap).
  </done>
</task>

<task type="auto">
  <name>Task 5: Add Graph Navigation Link</name>
  <files>src/App.tsx</files>
  <action>
Add navigation link to graph view in main navigation:
- Find existing navigation menu (likely in App.tsx or separate Navigation component)
- Add link to /graph route: "Graph" or "Knowledge Graph" label
- Icon: use network/graph icon if available (or generic icon)
- Place alongside existing navigation links (Notes, Chat, Library, Settings)

If navigation is in separate component, update that component instead. Follow existing navigation link pattern (styling, active state, etc.).
  </action>
  <verify>
    <automated>grep -c "graph" src/App.tsx | grep -v "^0$"</automated>
  </verify>
  <done>
Navigation link to /graph added to main navigation menu, graph accessible from application UI. Link follows existing navigation pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph data → WebGL renderer | Graph data rendered via Three.js InstancedMesh |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-06 | Denial of Service | Force simulation CPU usage | accept | Risk low - simulation stabilizes after initial layout, d3-force-3d has alpha decay, cooldown after 300 ticks |
| T-06-07 | Denial of Service | WebGL memory usage | accept | Risk low - InstancedMesh uses single geometry/material, memory bounded by node count, 1000 nodes ~10MB |
</threat_model>

<verification>
## Performance Verification (Manual)

After implementation, verify VIZ-02 requirement:
1. Create test database with 1000+ notes and links
2. Open /graph route
3. Open browser DevTools Performance tab
4. Record 10 seconds of graph interaction (rotate, zoom, pan)
5. Check frame rate: should be 50-60 FPS consistently
6. Check draw calls in WebGL inspector: should be ~1-5 (not 1000+)

Expected: Smooth 60 FPS with 1000+ nodes, single draw call for nodes.

## Automated Tests

```bash
npm test -- GraphView.test.ts --run
# Expected: All tests passing
# - Instanced rendering used for nodes
# - Force simulation configured with correct parameters
# - Neighbor highlighting works on node click
# - Highlighting clears on panel close
```

## Integration Checks

```bash
# Verify instanced rendering
grep -c "InstancedMesh" src/components/Graph/GraphView.tsx
# Expected: 1+ (instanced rendering implemented)

# Verify force configuration
grep -c "d3Force" src/components/Graph/GraphView.tsx
# Expected: 4+ (charge, link, center, collision forces)

# Verify navigation link
grep -c "graph" src/App.tsx
# Expected: 2+ (route + navigation link)
```
</verification>

<success_criteria>
- [ ] Instanced rendering implemented with InstancedMesh
- [ ] Node colors determined by tag hash (consistent colors)
- [ ] Low-poly sphere geometry (8 segments) for performance
- [ ] Force simulation tuned: charge -40, link distance 40, center 0.8, collision 10
- [ ] Neighbor highlighting on node click (white for selected, dimmed for others)
- [ ] Highlighted links styled differently (white, width 2)
- [ ] Highlighting clears when closing side panel
- [ ] GraphControls placeholder component created
- [ ] Navigation link to /graph added to main menu
- [ ] Manual performance test shows 60 FPS with 1000+ nodes
- [ ] All tests passing
</success_criteria>

<output>
Create `.planning/phases/06-3d-visualization/06-04-SUMMARY.md` when done
</output>
