---
phase: 06-3d-visualization
plan: 05
type: execute
wave: 5
depends_on: [06-04]
files_modified:
  - src/components/Graph/GraphView.tsx
  - src/components/Graph/GraphControls.tsx
  - src/components/Graph/GraphMinimap.tsx
  - src/hooks/useGraph.ts
autonomous: true
requirements: [VIZ-05]
user_setup: []

must_haves:
  truths:
    - "Graph updates automatically when new note created"
    - "New nodes animate into position smoothly"
    - "Search integration highlights matching nodes in graph"
    - "2D minimap shows top-down view of graph"
    - "Clicking minimap jumps camera to location"
  artifacts:
    - path: "src/components/Graph/GraphMinimap.tsx"
      provides: "2D top-down minimap component"
      exports: ["GraphMinimap"]
      min_lines: 40
    - path: "src/components/Graph/GraphControls.tsx"
      provides: "Search integration and controls"
      contains: "useSearch"
      min_lines: 60
  key_links:
    - from: "src/hooks/useGraph.ts"
      to: "window.api.notes.onCreated"
      via: "Real-time note creation listener"
      pattern: "onCreated"
    - from: "src/components/Graph/GraphControls.tsx"
      to: "src/hooks/useSearch.ts"
      via: "Search integration"
      pattern: "useSearch"
---

<objective>
Implement real-time graph updates, search integration, and minimap for complete 3D visualization experience.

Purpose: Complete VIZ-05 (graph updates when new knowledge added), integrate existing search (D-11), add 2D minimap (D-12).
Output: Real-time graph updates with smooth animations, search highlighting in graph, 2D minimap for navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-3d-visualization/06-CONTEXT.md
@.planning/phases/06-3d-visualization/06-RESEARCH.md
@src/hooks/useGraph.ts
@src/hooks/useSearch.ts
@src/components/Graph/GraphView.tsx
@src/components/Graph/GraphControls.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement Real-Time Graph Updates</name>
  <files>src/hooks/useGraph.ts, src/components/Graph/GraphView.tsx</files>
  <behavior>
    - useGraph listens for note creation events via window.api.notes.onCreated
    - New note added to graphData.nodes array
    - Graph re-renders with new node
    - d3-force-3d automatically animates new node into position (per D-18)
    - Listener cleaned up on unmount
  </behavior>
  <action>
Update useGraph.ts to listen for real-time note creation per RESEARCH.md Pattern 5:
- Add useEffect to subscribe to note creation events:
  ```typescript
  useEffect(() => {
    // Listen for note creation events from main process
    const unsubscribe = window.api.notes.onCreated((note) => {
      setGraphData(prev => ({
        ...prev,
        nodes: [...prev.nodes, { 
          id: note.id, 
          title: note.title, 
          tags: note.tags || [] 
        }]
      }));
    });
    
    return unsubscribe;
  }, []);
  ```
- Note: Links for new note will be added separately when auto-linking runs (Phase 5 feature)
- d3-force-3d automatically animates new nodes into position (no additional code needed per RESEARCH.md)

Check if window.api.notes.onCreated exists in preload.ts. If not, add it following existing event listener pattern (likely uses ipcRenderer.on).
  </action>
  <verify>
    <automated>npm test -- useGraph.test.ts --run</automated>
  </verify>
  <done>
Real-time graph updates implemented: new notes automatically appear in graph with smooth animation per D-18. VIZ-05 requirement fulfilled.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate Search with Graph Highlighting</name>
  <files>src/components/Graph/GraphControls.tsx, src/components/Graph/GraphView.tsx</files>
  <behavior>
    - GraphControls displays search input
    - Search uses existing useSearch hook (all modes: quickNav, fullText, fuzzy, semantic)
    - Matching nodes highlighted in graph (yellow/gold color)
    - Camera focuses on first search result
    - Search results cleared when input cleared
  </behavior>
  <action>
Update GraphControls.tsx to integrate search per RESEARCH.md Pattern 6 and D-11:
- Import useSearch from '../../hooks/useSearch'
- Import useState for search query
- Add search input field (Tailwind styled, placeholder "Search graph...")
- Call useSearch hook with query
- Pass searchResults to parent GraphView via callback prop
- Style: bg-background, p-4, rounded, shadow, absolute top-4 left-4

Update GraphView.tsx to handle search results:
- Accept searchResults prop from GraphControls
- Add searchMatchIds state (Set of matching node IDs)
- Update nodeColor logic:
  - If searchMatchIds.size > 0 and node in searchMatchIds: return '#fbbf24' (yellow/gold)
  - Else if highlightNodes.size > 0: existing highlight logic
  - Else: existing color logic
- Add useEffect to focus camera on first search result:
  ```typescript
  useEffect(() => {
    if (searchResults.length > 0 && fgRef.current) {
      const firstNode = graphData.nodes.find(n => n.id === searchResults[0].id);
      if (firstNode) {
        fgRef.current.cameraPosition(
          { x: firstNode.x, y: firstNode.y, z: firstNode.z + 200 },
          firstNode,
          1000 // Animation duration
        );
      }
    }
  }, [searchResults]);
  ```

Search integration reuses existing Phase 2 FTS search and Phase 5 semantic search per D-11.
  </action>
  <verify>
    <automated>npm test -- GraphControls.test.ts GraphView.test.ts --run</automated>
  </verify>
  <done>
Search integrated with graph: matching nodes highlighted in yellow, camera focuses on first result, uses existing search infrastructure per D-11.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create 2D Minimap Component</name>
  <files>src/components/Graph/GraphMinimap.tsx, src/components/Graph/GraphView.tsx</files>
  <behavior>
    - Minimap shows 2D top-down view of graph
    - Positioned in bottom-right corner
    - Shows all nodes as small dots
    - Current camera view highlighted with rectangle
    - Clicking minimap jumps camera to that location
    - Minimap updates as graph changes
  </behavior>
  <action>
Create GraphMinimap.tsx per D-12 and RESEARCH.md Open Question 1 (use separate ForceGraph2D instance):
- Import ForceGraph2D from 'react-force-graph-2d' (need to install this package first: npm install react-force-graph-2d)
- Props: { graphData: GraphData; onLocationClick: (x: number, y: number) => void }
- Component structure:
  - Container div: absolute bottom-4 right-4, w-48 h-48, bg-background/80, border, rounded, shadow
  - ForceGraph2D component:
    - graphData prop
    - width={192} height={192}
    - nodeRelSize={2} (small dots)
    - nodeColor={() => '#888'} (all gray)
    - linkColor={() => '#444'} (all dark gray)
    - enableZoomInteraction={false}
    - enablePanInteraction={false}
    - onNodeClick={(node) => onLocationClick(node.x, node.y)}
  - Overlay: "Minimap" label at top

Update GraphView.tsx to integrate minimap:
- Import GraphMinimap
- Add handleMinimapClick callback:
  ```typescript
  const handleMinimapClick = (x: number, y: number) => {
    if (fgRef.current) {
      fgRef.current.cameraPosition({ x, y, z: 200 }, { x, y, z: 0 }, 1000);
    }
  };
  ```
- Render minimap: <GraphMinimap graphData={graphData} onLocationClick={handleMinimapClick} />

Note: react-force-graph-2d is separate package from react-force-graph-3d, both by vasturiano. Add to package.json.
  </action>
  <verify>
    <automated>npm test -- GraphMinimap.test.ts --run</automated>
  </verify>
  <done>
2D minimap implemented per D-12: shows top-down view in corner, clicking jumps camera to location, updates with graph changes.
  </done>
</task>

<task type="auto">
  <name>Task 4: Install react-force-graph-2d for Minimap</name>
  <files>package.json, package-lock.json</files>
  <action>
Install react-force-graph-2d package for minimap component:

```bash
npm install react-force-graph-2d@latest
```

Verify installation:
- Check package.json contains react-force-graph-2d
- Check package-lock.json updated
- Run `npm list react-force-graph-2d` to confirm installation
- No peer dependency warnings

Note: This package is by same author (vasturiano) as react-force-graph-3d, provides 2D version of force-directed graph.
  </action>
  <verify>
    <automated>npm list react-force-graph-2d</automated>
  </verify>
  <done>
react-force-graph-2d installed successfully, ready for minimap component.
  </done>
</task>

<task type="auto">
  <name>Task 5: Add Cleanup and Memory Management</name>
  <files>src/components/Graph/GraphView.tsx</files>
  <action>
Add cleanup to prevent memory leaks per RESEARCH.md Pitfall 3:
- Add useEffect cleanup for ForceGraph3D:
  ```typescript
  useEffect(() => {
    return () => {
      if (fgRef.current) {
        fgRef.current.pauseAnimation();
      }
    };
  }, []);
  ```
- Ensure useGraph hook unsubscribes from note creation events (already done in Task 1)
- Dispose Three.js objects if creating custom geometries (already handled by shared geometry in Task 1 of Plan 04)

This prevents memory leaks when graph component unmounts, per RESEARCH.md Pitfall 3 warning.
  </action>
  <verify>
    <automated>grep -c "pauseAnimation" src/components/Graph/GraphView.tsx</automated>
  </verify>
  <done>
Memory management and cleanup implemented: animation paused on unmount, event listeners unsubscribed, no memory leaks.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| IPC events → Graph state | Real-time note creation events update graph |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of Service | Real-time updates flooding | accept | Risk low - note creation rate bounded by user interaction, no external input, local-first architecture |
| T-06-09 | Information Disclosure | Search results in graph | accept | Risk low - search shows user's own notes (already authorized), no sensitive data beyond what user created |
</threat_model>

<verification>
## Real-Time Updates Test

Manual verification:
1. Open /graph route
2. Create new note via /notes route
3. Verify new node appears in graph automatically
4. Verify node animates into position smoothly
5. No page refresh required

Expected: New node appears within 1 second, animates smoothly into graph layout.

## Search Integration Test

Manual verification:
1. Open /graph route
2. Type search query in GraphControls
3. Verify matching nodes highlighted in yellow
4. Verify camera focuses on first result
5. Clear search, verify highlighting removed

Expected: Search highlights nodes, camera moves smoothly, clearing works.

## Minimap Test

Manual verification:
1. Open /graph route with 20+ nodes
2. Verify minimap visible in bottom-right corner
3. Click location in minimap
4. Verify camera jumps to that location in main graph
5. Rotate main graph, verify minimap updates

Expected: Minimap shows full graph, clicking jumps camera, updates in real-time.

## Automated Tests

```bash
npm test -- useGraph.test.ts GraphControls.test.ts GraphMinimap.test.ts --run
# Expected: All tests passing
# - Real-time updates add nodes to graph
# - Search integration highlights matching nodes
# - Minimap renders 2D view
# - Minimap click triggers camera movement
```

## Memory Leak Check

Manual verification:
1. Open browser DevTools Memory profiler
2. Take heap snapshot
3. Open /graph route
4. Navigate away from /graph
5. Force garbage collection
6. Take another heap snapshot
7. Compare: ForceGraph3D instances should be released

Expected: No retained ForceGraph3D instances after navigation away.
</verification>

<success_criteria>
- [ ] useGraph listens for note creation events
- [ ] New notes automatically added to graph
- [ ] New nodes animate into position smoothly (d3-force-3d automatic)
- [ ] Event listeners cleaned up on unmount
- [ ] GraphControls displays search input
- [ ] Search uses existing useSearch hook (all modes)
- [ ] Matching nodes highlighted in yellow/gold
- [ ] Camera focuses on first search result
- [ ] Search clearing removes highlighting
- [ ] react-force-graph-2d installed
- [ ] GraphMinimap shows 2D top-down view
- [ ] Minimap positioned in bottom-right corner
- [ ] Clicking minimap jumps camera to location
- [ ] Minimap updates as graph changes
- [ ] Memory cleanup implemented (pauseAnimation on unmount)
- [ ] All tests passing
- [ ] Manual verification: real-time updates, search, minimap all working
</success_criteria>

<output>
Create `.planning/phases/06-3d-visualization/06-05-SUMMARY.md` when done
</output>
