---
phase: 06-3d-visualization
plan: 03
type: execute
wave: 3
depends_on: [06-02]
files_modified:
  - src/hooks/useGraph.ts
  - src/components/Graph/GraphView.tsx
  - src/components/Graph/GraphSidePanel.tsx
  - src/App.tsx
autonomous: true
requirements: [VIZ-01, VIZ-03, VIZ-04]
user_setup: []

must_haves:
  truths:
    - "useGraph hook fetches graph data via IPC"
    - "GraphView component renders 3D force-directed graph"
    - "Clicking node opens side panel with note content"
    - "Graph displays nodes as spheres colored by tag"
    - "Graph displays links as lines between nodes"
  artifacts:
    - path: "src/hooks/useGraph.ts"
      provides: "React hook for graph data fetching"
      exports: ["useGraph"]
      min_lines: 40
    - path: "src/components/Graph/GraphView.tsx"
      provides: "3D graph visualization component"
      exports: ["GraphView"]
      min_lines: 80
    - path: "src/components/Graph/GraphSidePanel.tsx"
      provides: "Side panel for note display"
      exports: ["GraphSidePanel"]
      min_lines: 30
  key_links:
    - from: "src/components/Graph/GraphView.tsx"
      to: "src/hooks/useGraph.ts"
      via: "useGraph hook call"
      pattern: "useGraph\\(\\)"
    - from: "src/components/Graph/GraphView.tsx"
      to: "react-force-graph-3d"
      via: "ForceGraph3D component"
      pattern: "ForceGraph3D"
    - from: "src/App.tsx"
      to: "src/components/Graph/GraphView.tsx"
      via: "Route for /graph"
      pattern: "path.*graph"
---

<objective>
Create React components for 3D graph visualization with basic interaction (node click, side panel).

Purpose: Render knowledge graph as interactive 3D neural network, enable navigation by clicking nodes.
Output: useGraph hook, GraphView component with ForceGraph3D, GraphSidePanel for note display, /graph route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-3d-visualization/06-CONTEXT.md
@.planning/phases/06-3d-visualization/06-RESEARCH.md
@src/hooks/useNotes.ts
@src/components/Notes/NoteEditor.tsx
@src/components/Notes/BacklinksPanel.tsx
@src/types/graph.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create useGraph Hook for Data Fetching</name>
  <files>src/hooks/useGraph.ts</files>
  <behavior>
    - useGraph() returns { graphData, loading, error, refetch }
    - Fetches graph data on mount via window.api.graph.getData()
    - graphData has type GraphData (nodes, links)
    - loading is true during fetch, false after
    - error populated on fetch failure
    - refetch function allows manual refresh
  </behavior>
  <action>
Create src/hooks/useGraph.ts following useNotes.ts pattern:
- Import useState, useEffect, useCallback from react
- Import GraphData, GraphNode, GraphLink from '../types/graph'
- Export function useGraph()
- State: graphData (initial: { nodes: [], links: [] }), loading (initial: true), error (initial: null)
- fetchGraph callback: try { setLoading(true); const data = await window.api.graph.getData(); setGraphData(data); setError(null); } catch (err) { setError(err); } finally { setLoading(false); }
- useEffect: call fetchGraph on mount
- Return { graphData, loading, error, refetch: fetchGraph }

Follow exact pattern from useNotes.ts (same structure, same error handling, same loading states).
  </action>
  <verify>
    <automated>npm test -- useGraph.test.ts --run</automated>
  </verify>
  <done>
useGraph hook fetches graph data via IPC, manages loading/error states, provides refetch function. Tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create GraphSidePanel Component</name>
  <files>src/components/Graph/GraphSidePanel.tsx</files>
  <behavior>
    - GraphSidePanel receives noteId and onClose props
    - Displays NoteEditor component for selected note
    - Has close button to dismiss panel
    - Positioned as fixed overlay on right side
    - Styled consistently with existing sidebar pattern
  </behavior>
  <action>
Create src/components/Graph/GraphSidePanel.tsx:
- Import NoteEditor from '../Notes/NoteEditor'
- Interface: GraphSidePanelProps { noteId: string; onClose: () => void }
- Component structure:
  - Fixed position overlay on right side (w-96, h-full, bg-background, border-l, shadow-lg)
  - Header with "Note" title and close button (X icon)
  - NoteEditor component with noteId prop
  - Close button calls onClose
- Styling: Use Tailwind classes, match existing sidebar styling (BacklinksPanel, RelatedPanel)
- Accessibility: Close button has aria-label="Close panel"

Follow existing panel patterns from BacklinksPanel.tsx and RelatedPanel.tsx for consistent styling.
  </action>
  <verify>
    <automated>npm test -- GraphSidePanel.test.ts --run</automated>
  </verify>
  <done>
GraphSidePanel displays NoteEditor for selected note, has close button, styled consistently with existing panels. Tests passing.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create GraphView Component with Basic 3D Rendering</name>
  <files>src/components/Graph/GraphView.tsx</files>
  <behavior>
    - GraphView renders ForceGraph3D with graph data
    - Shows loading state while fetching data
    - Nodes rendered as spheres (default rendering for now)
    - Links rendered as lines
    - Clicking node opens GraphSidePanel with note content
    - Camera controls enabled (orbit, zoom, pan)
    - Graph fills available space
  </behavior>
  <action>
Create src/components/Graph/GraphView.tsx following RESEARCH.md patterns:
- Import useGraph hook, ForceGraph3D from 'react-force-graph-3d', GraphSidePanel
- State: selectedNoteId (string | null)
- Call useGraph() to fetch data
- Loading state: return loading spinner/message
- Error state: return error message
- Main render:
  - Container div (relative, w-full, h-full)
  - ForceGraph3D component:
    - graphData prop from useGraph
    - nodeLabel="title" (show title on hover per D-08)
    - nodeAutoColorBy="tags" (color by tag per D-07)
    - onNodeClick handler: setSelectedNoteId(node.id)
    - enableNodeDrag={false} (per D-10, free camera only)
    - enableNavigationControls={true} (per D-10)
    - linkDirectionalParticles={0} (no particles per D-13)
  - Conditional GraphSidePanel: {selectedNoteId && <GraphSidePanel noteId={selectedNoteId} onClose={() => setSelectedNoteId(null)} />}

Use basic ForceGraph3D setup from RESEARCH.md Pattern 1. Advanced features (instanced rendering, force tuning, highlighting) deferred to next plan.
  </action>
  <verify>
    <automated>npm test -- GraphView.test.ts --run</automated>
  </verify>
  <done>
GraphView renders 3D force-directed graph with nodes and links, handles node clicks to open side panel, shows loading/error states. Tests passing.
  </done>
</task>

<task type="auto">
  <name>Task 4: Add Graph Route to Application</name>
  <files>src/App.tsx</files>
  <action>
Add /graph route to application routing:
- Import GraphView from './components/Graph/GraphView'
- Add Route: <Route path="/graph" element={<GraphView />} />
- Place route alongside existing routes (/notes, /chat, /library, etc.)
- Ensure route is within Router component

Verify route structure matches existing patterns in App.tsx. No navigation link added yet (deferred to next plan for full integration).
  </action>
  <verify>
    <automated>grep -c 'path="/graph"' src/App.tsx</automated>
  </verify>
  <done>
/graph route added to application, GraphView component accessible at /graph path. Route follows existing pattern.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Graph data → Three.js renderer | Graph data from IPC rendered in WebGL context |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | Node labels (XSS) | accept | Risk low - react-force-graph-3d uses DOM text nodes (not innerHTML), React escapes strings by default |
| T-06-05 | Denial of Service | Large graph rendering | accept | Risk low - graph size bounded by user's note count, WebGL handles 1000+ nodes, instanced rendering in next plan |
</threat_model>

<verification>
## Hook Tests

```bash
npm test -- useGraph.test.ts --run
# Expected: All tests passing
# - Fetches graph data on mount
# - Returns { graphData, loading, error, refetch }
# - Loading state managed correctly
# - Error state populated on failure
# - Refetch function works
```

## Component Tests

```bash
npm test -- GraphSidePanel.test.ts --run
# Expected: All tests passing
# - Renders NoteEditor with noteId
# - Close button calls onClose
# - Styled consistently

npm test -- GraphView.test.ts --run
# Expected: All tests passing
# - Renders ForceGraph3D with graph data
# - Shows loading state while fetching
# - Node click opens side panel
# - Side panel closes on close button click
```

## Route Verification

```bash
grep -c 'path="/graph"' src/App.tsx
# Expected: 1 (route registered)
```
</verification>

<success_criteria>
- [ ] useGraph hook fetches graph data via window.api.graph.getData()
- [ ] useGraph returns graphData, loading, error, refetch
- [ ] GraphSidePanel displays NoteEditor for selected note
- [ ] GraphSidePanel has close button
- [ ] GraphView renders ForceGraph3D component
- [ ] GraphView shows loading state while fetching
- [ ] Clicking node opens GraphSidePanel with note content
- [ ] Graph displays nodes as spheres (default rendering)
- [ ] Graph displays links as lines
- [ ] Camera controls enabled (orbit, zoom, pan)
- [ ] /graph route added to App.tsx
- [ ] All tests passing
</success_criteria>

<output>
Create `.planning/phases/06-3d-visualization/06-03-SUMMARY.md` when done
</output>
