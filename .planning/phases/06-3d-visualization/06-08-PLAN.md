---
phase: 06-3d-visualization
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/Graph/GraphMinimap.tsx
autonomous: true
requirements: [VIZ-04]
gap_closure: true

must_haves:
  truths:
    - "Minimap displays nodes as visible dots in 2D top-down view"
    - "Clicking minimap nodes jumps main camera to that location"
  artifacts:
    - path: "src/components/Graph/GraphMinimap.tsx"
      provides: "Working ForceGraph2D minimap with visible nodes"
      min_lines: 30
  key_links:
    - from: "GraphMinimap component"
      to: "ForceGraph2D"
      via: "graphData prop and node rendering"
      pattern: "ForceGraph2D.*graphData"
---

<objective>
Fix minimap rendering so nodes are visible and clickable.

Purpose: Minimap shows border but no content, not clickable (UAT Test 12, major severity).

Output: Working minimap with visible nodes and click navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-3d-visualization/06-UAT.md
@src/components/Graph/GraphMinimap.tsx
@src/components/Graph/GraphView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix ForceGraph2D rendering in minimap</name>
  <files>src/components/Graph/GraphMinimap.tsx</files>
  <action>
Debug and fix ForceGraph2D rendering issues in GraphMinimap component:

**Likely root causes:**
1. ForceGraph2D not receiving valid graphData (nodes/links empty or malformed)
2. Canvas not rendering due to size constraints or CSS issues
3. ForceGraph2D needs explicit cooldown or warmup configuration
4. Background transparency hiding nodes

**Fixes to apply:**

1. Add debug logging to verify graphData has nodes:
   ```typescript
   console.log('Minimap graphData:', graphData.nodes.length, 'nodes', graphData.links.length, 'links');
   ```

2. Ensure ForceGraph2D has explicit dimensions and canvas rendering:
   - Verify width={192} height={192} props are applied
   - Add `nodeCanvasObject` prop for explicit canvas rendering if needed
   - Remove `backgroundColor="transparent"` if it's hiding nodes (try '#1a1a1a' or '#ffffff')

3. Add cooldown configuration to prevent infinite simulation:
   ```typescript
   cooldownTicks={100}
   cooldownTime={3000}
   ```

4. Increase nodeRelSize from 2 to 4 or 5 for better visibility

5. Verify onNodeClick receives correct node object with x, y coordinates

6. Add ref to ForceGraph2D and call ref.zoomToFit() after mount to ensure all nodes visible

Implementation approach:
- Start with background color change (transparent → solid)
- Add cooldown configuration
- Increase node size
- Add ref and zoomToFit call
- Test each change incrementally
  </action>
  <verify>
    <automated>npm test -- GraphMinimap.test.tsx</automated>
  </verify>
  <done>Minimap displays nodes as visible dots. Clicking nodes triggers onLocationClick with correct coordinates. ForceGraph2D renders properly in 192x192 container. Tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User click → camera navigation | Minimap clicks trigger camera movement |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08-01 | Denial of Service | Minimap force simulation | mitigate | Add cooldownTicks and cooldownTime to prevent infinite simulation consuming CPU. |
| T-06-08-02 | Information Disclosure | Graph structure visibility | accept | Minimap shows graph structure but no sensitive content. Low risk. |
</threat_model>

<verification>
1. Run GraphMinimap tests: `npm test -- GraphMinimap.test.tsx`
2. Manual test: Open /graph, verify minimap shows gray dots for nodes
3. Manual test: Click node in minimap, verify main camera jumps to location
4. Verify minimap updates when graph data changes
</verification>

<success_criteria>
- Minimap displays nodes as visible gray dots
- Minimap shows links as dark gray lines
- Clicking minimap nodes triggers camera navigation
- ForceGraph2D renders in 192x192 container
- All GraphMinimap tests pass
- UAT Test 12 major issue resolved
</success_criteria>

<output>
Create `.planning/phases/06-3d-visualization/06-08-SUMMARY.md` when done
</output>
