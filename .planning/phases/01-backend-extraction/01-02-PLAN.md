---
phase: 01-backend-extraction
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - electron/database/connection.ts
  - src/config/appConfig.ts
  - .env.example
autonomous: true
requirements:
  - BACK-03
  - BACK-05
must_haves:
  truths:
    - Database path resolves from environment variables without Electron APIs
    - Config storage reads/writes JSON files without electron-store
    - Environment variables override config file values
  artifacts:
    - path: electron/database/connection.ts
      provides: Database initialization with Node.js path resolution
      exports: ["initDatabase", "getORM"]
      contains: "getDatabasePath()"
    - path: src/config/appConfig.ts
      provides: File-based config management
      exports: ["loadConfig", "saveConfig", "updateConfig"]
    - path: .env.example
      provides: Environment variable template
      contains: "LIBRANIA_DB_PATH"
  key_links:
    - from: electron/database/connection.ts
      to: process.env
      via: "getDatabasePath() reads LIBRANIA_DB_PATH"
      pattern: "process\\.env\\.LIBRANIA_DB_PATH"
    - from: src/config/appConfig.ts
      to: filesystem
      via: "fs.readFile/writeFile for config.json"
      pattern: "fs\\.(readFile|writeFile)"
---

<objective>
Migrate database and config storage from Electron APIs to Node.js equivalents: replace app.getPath('userData') with environment variable-based path resolution, ensure config management works without electron-store, and establish .env file pattern for configuration.

Purpose: Removes Electron dependencies from data layer — database and config work in standalone Node.js server.

Output: Database and config systems ready for use by HTTP routes in Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-extraction/01-CONTEXT.md
@.planning/phases/01-backend-extraction/01-RESEARCH.md
@.planning/phases/01-backend-extraction/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update database connection with Node.js path resolution</name>
  <files>electron/database/connection.ts</files>
  <read_first>
    - electron/database/connection.ts (current implementation)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (path resolution pattern lines 599-624)
  </read_first>
  <action>
Update electron/database/connection.ts per D-03 (database operations without Electron APIs). Add getDatabasePath() function with priority: process.env.LIBRANIA_DB_PATH > process.env.LIBRANIA_DATA_DIR + '/librania.db' > process.cwd() + '/data/librania.db'. Import fs.promises for directory creation. In initDatabase(), call getDatabasePath() to get dbPath, then ensure parent directory exists with `await fs.mkdir(path.dirname(dbPath), { recursive: true })` before creating Database instance. Remove any Electron app.getPath() calls if present. Pattern from PATTERNS.md lines 599-624. Keep all existing database initialization logic (schema, migrations, extensions).
  </action>
  <acceptance_criteria>
    - electron/database/connection.ts contains getDatabasePath() function
    - Function checks process.env.LIBRANIA_DB_PATH first
    - Function checks process.env.LIBRANIA_DATA_DIR second
    - Function falls back to process.cwd() + '/data/librania.db'
    - initDatabase() calls getDatabasePath() and creates parent directory
    - File does not contain 'electron' import or app.getPath references
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Database path resolution migrated to environment variables, no Electron dependencies</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Verify config management works without electron-store</name>
  <files>src/config/appConfig.ts, src/config/appConfig.test.ts</files>
  <read_first>
    - src/config/appConfig.ts (existing implementation)
    - .planning/phases/01-backend-extraction/01-CONTEXT.md (D-08, D-09: keep JSON format, store in project root)
  </read_first>
  <behavior>
    - Test 1: loadConfig() reads config.json from filesystem and returns parsed object
    - Test 2: saveConfig() writes config object to config.json as formatted JSON
    - Test 3: updateConfig() merges partial updates with existing config
    - Test 4: loadConfig() returns default config if file doesn't exist
    - Test 5: Config path resolves to process.cwd() + '/config.json' (not Electron userData)
  </behavior>
  <action>
Review src/config/appConfig.ts per D-08 and D-09 (keep JSON format, store in project root). Verify loadConfig/saveConfig/updateConfig functions use Node.js fs module (not electron-store). Ensure config path is `path.join(process.cwd(), 'config.json')` per D-09. If any electron-store imports exist, remove them and replace with fs.readFile/fs.writeFile. Create or update test file with 5 behavior tests. Pattern: config already implemented per CONTEXT.md "Config utilities already exist", just verify no Electron dependencies and add tests.
  </action>
  <acceptance_criteria>
    - src/config/appConfig.ts does not import 'electron' or 'electron-store'
    - File uses fs.readFile and fs.writeFile for config.json
    - Config path is process.cwd() + '/config.json'
    - src/config/appConfig.test.ts exists with 5 test cases
    - `npm test -- src/config/appConfig.test.ts -x` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Config management verified to work without electron-store, tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create .env.example template</name>
  <files>.env.example</files>
  <read_first>
    - .planning/phases/01-backend-extraction/01-RESEARCH.md (environment variable configuration lines 682-707)
    - .planning/phases/01-backend-extraction/01-CONTEXT.md (D-10: env vars override config)
  </read_first>
  <action>
Create .env.example per D-10 (environment variables override config file values). Include sections: Server configuration (LIBRANIA_PORT=3000, LIBRANIA_HOST=localhost, CORS_ORIGIN=http://localhost:5173), Data paths (LIBRANIA_DATA_DIR=./data, LIBRANIA_DB_PATH=./data/librania.db, LIBRANIA_UPLOAD_DIR=./data/uploads), Security (LIBRANIA_MASTER_KEY=<generate-random-key>), AI Provider API Keys (CLAUDE_API_KEY, CLAUDE_MODEL, OPENAI_API_KEY, OPENAI_MODEL, DEEPSEEK_API_KEY, DEEPSEEK_MODEL), Optional (TAVILY_API_KEY). Add comments explaining each variable. Pattern from RESEARCH.md lines 682-707. This is a template file — actual .env file is gitignored and user-created.
  </action>
  <acceptance_criteria>
    - .env.example exists in project root
    - File contains LIBRANIA_PORT, LIBRANIA_HOST, CORS_ORIGIN
    - File contains LIBRANIA_DATA_DIR, LIBRANIA_DB_PATH, LIBRANIA_UPLOAD_DIR
    - File contains LIBRANIA_MASTER_KEY with placeholder
    - File contains AI provider variables (CLAUDE_API_KEY, OPENAI_API_KEY, DEEPSEEK_API_KEY)
    - File contains comments explaining each variable
  </acceptance_criteria>
  <done>.env.example created with all configuration variables documented</done>
</task>

<task type="auto">
  <name>Task 4: Load environment variables in server startup</name>
  <files>electron/server.ts</files>
  <read_first>
    - electron/server.ts (current implementation)
    - .env.example (newly created template)
  </read_first>
  <action>
Update electron/server.ts to load environment variables at startup. Add `import 'dotenv/config'` at the top of the file (before other imports). This auto-loads .env file if present. Update port parameter default to use `process.env.LIBRANIA_PORT ? parseInt(process.env.LIBRANIA_PORT) : 3000`. No other changes needed — dotenv/config side-effect import handles .env loading. Pattern: dotenv 17.4.2 supports /config import for automatic loading.
  </action>
  <acceptance_criteria>
    - electron/server.ts contains `import 'dotenv/config'` as first import
    - startServer function uses process.env.LIBRANIA_PORT for default port
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Environment variables loaded via dotenv at server startup</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| .env file → application | Sensitive credentials cross from file to memory |
| Filesystem → config.json | User-editable config crosses to application state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Information Disclosure | .env file | mitigate | .env file gitignored, .env.example provided as template, no secrets committed |
| T-01-06 | Tampering | config.json | accept | Local-first app, user controls filesystem, no remote config injection risk |
| T-01-07 | Information Disclosure | Database file | accept | Local-first app, database stored in user's data directory, no network exposure |
</threat_model>

<verification>
## Automated Checks

```bash
# TypeScript compilation
npm run build

# Config tests pass
npm test -- src/config/appConfig.test.ts -x

# Database path resolution works (integration test in Wave 2)
# Server loads .env variables (integration test in Wave 2)
```

## Manual Verification

- Database path resolves from LIBRANIA_DB_PATH env var
- Config functions work without electron-store
- .env.example documents all configuration variables
- dotenv loads environment variables at server startup
</verification>

<success_criteria>
- Database path resolution uses environment variables (BACK-03 complete)
- Config storage works without electron-store (BACK-05 complete)
- Environment variables override config file values (D-10 implemented)
- All tests pass and TypeScript compiles
</success_criteria>

<output>
Create `.planning/phases/01-backend-extraction/01-02-SUMMARY.md` when done
</output>
