---
phase: 01-backend-extraction
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - electron/api/notes.routes.ts
  - electron/api/content.routes.ts
  - electron/api/search.routes.ts
  - electron/api/tags.routes.ts
  - electron/api/export.routes.ts
  - electron/api/config.routes.ts
  - electron/api/routes.ts
autonomous: true
requirements:
  - BACK-02
  - BACK-04
must_haves:
  truths:
    - Notes CRUD operations work via HTTP endpoints
    - Content upload works via HTTP multipart with Multer
    - Search queries work via HTTP endpoints
    - Tag management works via HTTP endpoints
    - Export operations work via HTTP endpoints
    - Config management works via HTTP endpoints
  artifacts:
    - path: electron/api/notes.routes.ts
      provides: Notes CRUD endpoints
      exports: ["default"]
      min_lines: 80
    - path: electron/api/content.routes.ts
      provides: Content upload endpoint with Multer
      exports: ["default"]
      contains: "upload.single"
    - path: electron/api/search.routes.ts
      provides: Search endpoints
      exports: ["default"]
    - path: electron/api/tags.routes.ts
      provides: Tag management endpoints
      exports: ["default"]
    - path: electron/api/export.routes.ts
      provides: Export endpoints
      exports: ["default"]
    - path: electron/api/config.routes.ts
      provides: Config management endpoints
      exports: ["default"]
  key_links:
    - from: electron/api/notes.routes.ts
      to: electron/services/notes.service.ts
      via: "import service functions"
      pattern: "import.*notes\\.service"
    - from: electron/api/content.routes.ts
      to: electron/middleware/upload.middleware.ts
      via: "import upload middleware"
      pattern: "import.*upload.*middleware"
    - from: electron/api/routes.ts
      to: "all new route files"
      via: "router.use() registration"
      pattern: "router\\.use.*routes"
---

<objective>
Convert 6 IPC handler files to HTTP REST endpoints: notes, content, search, tags, export, and config. Each handler file becomes one route file following 1:1 mapping per D-04. Preserve service layer calls, add HTTP-specific concerns (status codes, validation, error handling).

Purpose: Completes IPC-to-HTTP migration for non-streaming operations — all CRUD and query operations accessible via REST API.

Output: 6 new route files integrated with Express server, IPC handlers replaced.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-extraction/01-CONTEXT.md
@.planning/phases/01-backend-extraction/01-RESEARCH.md
@.planning/phases/01-backend-extraction/01-PATTERNS.md
@electron/api/routes.ts
@electron/ipc/notes.handlers.ts
@electron/ipc/content.handlers.ts
@electron/ipc/search.handlers.ts
@electron/ipc/tags.handlers.ts
@electron/ipc/export.handlers.ts
@electron/ipc/config.handlers.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert notes IPC handlers to HTTP routes</name>
  <files>electron/api/notes.routes.ts, electron/api/notes.routes.test.ts</files>
  <read_first>
    - electron/ipc/notes.handlers.ts (IPC handlers to convert)
    - electron/api/routes.ts (existing route patterns lines 70-136)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (IPC-to-HTTP conversion lines 35-114, shared patterns lines 636-716)
    - electron/services/notes.service.ts (service functions to call)
  </read_first>
  <behavior>
    - Test 1: POST /api/notes creates note and returns 201 with note object
    - Test 2: GET /api/notes returns 200 with array of all notes
    - Test 3: GET /api/notes/:id returns 200 with note object or 404 if not found
    - Test 4: PUT /api/notes/:id updates note and returns 200 with updated note
    - Test 5: DELETE /api/notes/:id deletes note and returns 200
    - Test 6: POST /api/notes with missing title returns 400 with error message
  </behavior>
  <action>
Create electron/api/notes.routes.ts per D-04 (1:1 handler-to-route mapping). Import Router from express, service functions from ../services/notes.service.ts, getORM from ../database/connection. Create router instance. Map each IPC handler to HTTP route: 'notes:create' → POST /api/notes, 'notes:getAll' → GET /api/notes, 'notes:getById' → GET /api/notes/:id, 'notes:update' → PUT /api/notes/:id, 'notes:delete' → DELETE /api/notes/:id. Each route: get db with getORM(), call service function, return { success: true, data } on success or { success: false, error } with appropriate status code (400 for validation, 404 for not found, 500 for errors). Add logging middleware. Export router as default. Pattern from PATTERNS.md lines 35-114 and 636-716. Create test file with 6 behavior tests using Vitest and supertest.
  <verify>
    <automated>npm test -- electron/api/notes.routes.test.ts -x</automated>
  </verify>
  </action>
  <acceptance_criteria>
    - electron/api/notes.routes.ts exists with 80+ lines
    - File exports default router
    - File contains POST /api/notes, GET /api/notes, GET /api/notes/:id, PUT /api/notes/:id, DELETE /api/notes/:id
    - Each route calls service function from notes.service.ts
    - Each route returns { success: true/false } JSON response
    - electron/api/notes.routes.test.ts exists with 6 test cases
    - `npm test -- electron/api/notes.routes.test.ts -x` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Notes routes created with 5 CRUD endpoints, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert content IPC handlers to HTTP routes with file upload</name>
  <files>electron/api/content.routes.ts, electron/api/content.routes.test.ts</files>
  <read_first>
    - electron/ipc/content.handlers.ts (IPC handlers to convert)
    - electron/api/routes.ts (existing route patterns)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (file upload pattern lines 199-234, Multer usage lines 482-502)
    - electron/services/content.service.ts (createContent function)
    - electron/middleware/upload.middleware.ts (upload middleware from Plan 01)
  </read_first>
  <behavior>
    - Test 1: POST /api/content/upload with valid file returns 201 with content object
    - Test 2: POST /api/content/upload without file returns 400 with error
    - Test 3: POST /api/content/upload with invalid file type returns 400 with error
    - Test 4: GET /api/content returns 200 with array of all content
    - Test 5: GET /api/content/:id returns 200 with content object or 404 if not found
    - Test 6: DELETE /api/content/:id deletes content and returns 200
  <verify>
    <automated>npm test -- electron/api/content.routes.test.ts -x</automated>
  </verify>
  </behavior>
  <action>
Create electron/api/content.routes.ts per D-04. Import Router, upload middleware from ../middleware/upload.middleware, service functions from ../services/content.service.ts, getORM. Map handlers: 'content:create' → POST /api/content/upload with upload.single('file') middleware, 'content:getAll' → GET /api/content, 'content:getById' → GET /api/content/:id, 'content:delete' → DELETE /api/content/:id. Upload route: check req.file exists (400 if missing), call createContent with { filePath: req.file.path, source: 'upload' }, return 201. Other routes follow standard CRUD pattern. Pattern from PATTERNS.md lines 199-234 and 482-502. Create test file with 6 behavior tests (use mock files for upload tests).
  </action>
  <acceptance_criteria>
    - electron/api/content.routes.ts exists
    - File exports default router
    - File contains POST /api/content/upload with upload.single('file')
    - File contains GET /api/content, GET /api/content/:id, DELETE /api/content/:id
    - Upload route checks req.file and returns 400 if missing
    - electron/api/content.routes.test.ts exists with 6 test cases
    - `npm test -- electron/api/content.routes.test.ts -x` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Content routes created with file upload endpoint using Multer, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert search, tags, export, and config IPC handlers to HTTP routes</name>
  <files>electron/api/search.routes.ts, electron/api/tags.routes.ts, electron/api/export.routes.ts, electron/api/config.routes.ts, electron/api/search.routes.test.ts, electron/api/tags.routes.test.ts, electron/api/export.routes.test.ts, electron/api/config.routes.test.ts</files>
  <read_first>
    - electron/ipc/search.handlers.ts (search handlers)
    - electron/ipc/tags.handlers.ts (tag handlers)
    - electron/ipc/export.handlers.ts (export handlers)
    - electron/ipc/config.handlers.ts (config handlers)
    - electron/api/routes.ts (existing route patterns)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (IPC-to-HTTP conversion, shared patterns)
    - electron/services/search.service.ts, electron/services/tags.service.ts (service functions)
    - src/config/appConfig.ts (config functions)
  </read_first>
  <behavior>
    Search routes:
    - Test 1: POST /api/search with query returns 200 with search results
    - Test 2: POST /api/search without query returns 400
    
    Tags routes:
    - Test 3: GET /api/tags returns 200 with array of all tags
    - Test 4: POST /api/tags creates tag and returns 201
    - Test 5: DELETE /api/tags/:id deletes tag and returns 200
    
    Export routes:
    - Test 6: POST /api/export/notes exports notes and returns 200 with file path
    
    Config routes:
    - Test 7: GET /api/config returns 200 with config object
    - Test 8: PUT /api/config updates config and returns 200
  </behavior>
  <action>
Create 4 route files per D-04 (1:1 handler-to-route mapping). Each file: import Router, service functions, getORM. Map IPC handlers to HTTP routes following patterns from PATTERNS.md.

Search routes: POST /api/search (body: { query, type }), POST /api/search/semantic (body: { query, limit }).

Tags routes: GET /api/tags, POST /api/tags (body: { name, color }), PUT /api/tags/:id, DELETE /api/tags/:id.
  <verify>
    <automated>npm test -- electron/api/search.routes.test.ts electron/api/tags.routes.test.ts electron/api/export.routes.test.ts electron/api/config.routes.test.ts -x</automated>
  </verify>

Export routes: POST /api/export/notes (body: { format, noteIds }), POST /api/export/library.

Config routes: GET /api/config, PUT /api/config (body: partial config object), POST /api/config/reset.

Each route: standard error handling, validation, service layer calls, { success, data/error } responses. Create test file for each with behavior tests covering happy path and error cases.
  </action>
  <acceptance_criteria>
    - electron/api/search.routes.ts exists with POST /api/search, POST /api/search/semantic
    - electron/api/tags.routes.ts exists with GET /api/tags, POST /api/tags, PUT /api/tags/:id, DELETE /api/tags/:id
    - electron/api/export.routes.ts exists with POST /api/export/notes, POST /api/export/library
    - electron/api/config.routes.ts exists with GET /api/config, PUT /api/config, POST /api/config/reset
    - All 4 files export default router
    - Test files exist for all 4 route files with behavior tests
    - `npm test -- electron/api/*.routes.test.ts -x` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Search, tags, export, and config routes created, all tests pass</done>
</task>

<task type="auto">
  <name>Task 4: Register new routes in main routes file</name>
  <files>electron/api/routes.ts</files>
  <read_first>
    - electron/api/routes.ts (current routes)
    - electron/api/notes.routes.ts (new routes to register)
    - electron/api/content.routes.ts (new routes to register)
    - electron/api/search.routes.ts (new routes to register)
    - electron/api/tags.routes.ts (new routes to register)
    - electron/api/export.routes.ts (new routes to register)
    - electron/api/config.routes.ts (new routes to register)
  </read_first>
  <action>
Update electron/api/routes.ts to register all new route files. Import each route file: `import notesRoutes from './notes.routes'`, `import contentRoutes from './content.routes'`, etc. Add router.use() calls after existing routes: `router.use(notesRoutes)`, `router.use(contentRoutes)`, `router.use(searchRoutes)`, `router.use(tagsRoutes)`, `router.use(exportRoutes)`, `router.use(configRoutes)`. Keep existing routes (providers, conversations, chat) — these will be migrated in Plan 04. Add comment: "// IPC-to-HTTP converted routes (Plan 03)". Pattern: routes.ts already uses router.use() pattern for modular routes.
  </action>
  <acceptance_criteria>
    - electron/api/routes.ts imports all 6 new route files
    - File contains router.use(notesRoutes), router.use(contentRoutes), etc.
    - Existing routes (providers, conversations, chat) remain unchanged
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>All new routes registered in main routes file</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| HTTP client → API routes | Untrusted browser requests cross here |
| File upload → content service | Untrusted file content crosses to storage |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-08 | Tampering | POST /api/notes | mitigate | Input validation on required fields (title), service layer validates data types |
| T-01-09 | Tampering | POST /api/content/upload | mitigate | Multer file type validation (from Plan 01), size limit 10MB |
| T-01-10 | Information Disclosure | GET /api/config | accept | Local-first app, config contains no secrets (API keys encrypted separately) |
| T-01-11 | Denial of Service | Search endpoints | accept | Local database, no remote query injection risk, SQLite handles malformed queries |
</threat_model>

<verification>
## Automated Checks

```bash
# All route tests pass
npm test -- electron/api/*.routes.test.ts -x

# TypeScript compilation
npm run build

# Integration test: server starts and routes respond (manual in Wave 3)
```

## Manual Verification

- Notes CRUD endpoints work via HTTP
- Content upload endpoint accepts files via Multer
- Search, tags, export, config endpoints work via HTTP
- All routes return consistent { success, data/error } format
</verification>

<success_criteria>
- 6 IPC handler files converted to HTTP routes (BACK-02 partial)
- File upload works via Multer middleware (BACK-04 complete)
- All routes integrated with Express server
- All tests pass (30+ behavior tests across 6 route files)
</success_criteria>

<output>
Create `.planning/phases/01-backend-extraction/01-03-SUMMARY.md` when done
</output>
