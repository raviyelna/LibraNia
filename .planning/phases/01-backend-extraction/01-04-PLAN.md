---
phase: 01-backend-extraction
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - electron/websocket/socket.handlers.ts
  - electron/api/graph.routes.ts
  - electron/server.ts
  - electron/api/routes.ts
autonomous: true
requirements:
  - BACK-02
must_haves:
  truths:
    - AI chat streams tokens via WebSocket in real-time
    - Graph updates emit via WebSocket to subscribed clients
    - Graph CRUD operations work via HTTP endpoints
    - WebSocket handlers integrated with Socket.IO server
  artifacts:
    - path: electron/websocket/socket.handlers.ts
      provides: Socket.IO event handlers for AI streaming and graph updates
      exports: ["setupSocketHandlers"]
      min_lines: 80
    - path: electron/api/graph.routes.ts
      provides: Graph CRUD endpoints
      exports: ["default"]
    - path: electron/server.ts
      provides: Socket.IO handlers registered at startup
      contains: "setupSocketHandlers(io)"
  key_links:
    - from: electron/websocket/socket.handlers.ts
      to: electron/ipc/ai.handlers.ts
      via: "import AI provider functions"
      pattern: "import.*ai\\.handlers"
    - from: electron/server.ts
      to: electron/websocket/socket.handlers.ts
      via: "setupSocketHandlers(io) call"
      pattern: "setupSocketHandlers\\(io\\)"
    - from: electron/websocket/socket.handlers.ts
      to: socket.io
      via: "io.on('connection') event handling"
      pattern: "io\\.on\\('connection'"
---

<objective>
Implement WebSocket handlers for AI streaming and graph updates using Socket.IO per D-05 and D-07. Convert graph IPC handlers to HTTP routes. Integrate WebSocket handlers with Express server from Plan 01.

Purpose: Completes IPC-to-HTTP migration for streaming operations — AI chat tokens and graph updates stream in real-time via WebSocket, graph CRUD via HTTP.

Output: WebSocket handlers operational, all IPC handlers converted to HTTP/WebSocket.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-extraction/01-CONTEXT.md
@.planning/phases/01-backend-extraction/01-RESEARCH.md
@.planning/phases/01-backend-extraction/01-PATTERNS.md
@electron/server.ts
@electron/ipc/ai.handlers.ts
@electron/ipc/graph.handlers.ts
@electron/api/routes.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Socket.IO event handlers for AI streaming</name>
  <files>electron/websocket/socket.handlers.ts, electron/websocket/socket.handlers.test.ts</files>
  <read_first>
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (Socket.IO pattern lines 298-347)
    - electron/ipc/ai.handlers.ts (AI provider functions to reuse)
    - electron/api/routes.ts (existing chat endpoint lines 156-227)
  </read_first>
  <behavior>
    - Test 1: 'ai:chat' event with valid data streams tokens via 'ai:token' events
    - Test 2: 'ai:chat' event emits 'ai:complete' when streaming finishes
    - Test 3: 'ai:chat' event emits 'ai:error' if provider not configured
    - Test 4: 'ai:chat' event emits 'ai:error' if AI call fails
    - Test 5: 'graph:subscribe' event joins client to graph room
    - Test 6: 'disconnect' event logs client disconnection
  </behavior>
  <action>
Create electron/websocket/socket.handlers.ts per D-05 and D-07 (WebSocket for AI streaming, Socket.IO implementation). Export setupSocketHandlers(io: Server) function. Inside: io.on('connection', (socket) => { ... }). Handle 'ai:chat' event: extract { conversationId, messages, providerId, model } from data, load provider config, call AI provider with streaming callback that emits 'ai:token' events, emit 'ai:complete' on finish, emit 'ai:error' on failure. Handle 'graph:subscribe' event: socket.join(`graph:${data.graphId}`). Handle 'disconnect' event: log disconnection. Import AI provider functions from ../ipc/ai.handlers.ts (callClaude, callOpenAI, callDeepSeek). Pattern from PATTERNS.md lines 298-347. Create test file with 6 behavior tests using socket.io-client for testing.
  <verify>
    <automated>npm test -- electron/websocket/socket.handlers.test.ts -x</automated>
  </verify>
  </action>
  <acceptance_criteria>
    - electron/websocket/socket.handlers.ts exists with 80+ lines
    - File exports setupSocketHandlers function
    - Function handles 'ai:chat', 'graph:subscribe', 'disconnect' events
    - 'ai:chat' handler emits 'ai:token' for each token, 'ai:complete' on finish, 'ai:error' on failure
    - 'graph:subscribe' handler calls socket.join() with room name
    - electron/websocket/socket.handlers.test.ts exists with 6 test cases
    - `npm test -- electron/websocket/socket.handlers.test.ts -x` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Socket.IO handlers created for AI streaming and graph subscriptions, all tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert graph IPC handlers to HTTP routes</name>
  <files>electron/api/graph.routes.ts, electron/api/graph.routes.test.ts</files>
  <read_first>
    - electron/ipc/graph.handlers.ts (IPC handlers to convert)
    - electron/api/routes.ts (existing route patterns)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (IPC-to-HTTP conversion, shared patterns)
    - electron/services/graph.service.ts (service functions to call)
  </read_first>
  <behavior>
    - Test 1: GET /api/graph returns 200 with graph data (nodes and edges)
    - Test 2: POST /api/graph/nodes creates node and returns 201
    - Test 3: PUT /api/graph/nodes/:id updates node and returns 200
    - Test 4: DELETE /api/graph/nodes/:id deletes node and returns 200
    - Test 5: POST /api/graph/edges creates edge and returns 201
    - Test 6: DELETE /api/graph/edges/:id deletes edge and returns 200
  </behavior>
  <verify>
    <automated>npm test -- electron/api/graph.routes.test.ts -x</automated>
  </verify>
  <action>
Create electron/api/graph.routes.ts per D-04 (1:1 handler-to-route mapping). Import Router, service functions from ../services/graph.service.ts, getORM. Map IPC handlers to HTTP routes: 'graph:get' → GET /api/graph, 'graph:createNode' → POST /api/graph/nodes, 'graph:updateNode' → PUT /api/graph/nodes/:id, 'graph:deleteNode' → DELETE /api/graph/nodes/:id, 'graph:createEdge' → POST /api/graph/edges, 'graph:deleteEdge' → DELETE /api/graph/edges/:id. Each route: get db with getORM(), call service function, return { success: true, data } on success or { success: false, error } with appropriate status code. Add logging middleware. Export router as default. Pattern from PATTERNS.md IPC-to-HTTP conversion. Create test file with 6 behavior tests.
  </action>
  <acceptance_criteria>
    - electron/api/graph.routes.ts exists
    - File exports default router
    - File contains GET /api/graph, POST /api/graph/nodes, PUT /api/graph/nodes/:id, DELETE /api/graph/nodes/:id, POST /api/graph/edges, DELETE /api/graph/edges/:id
    - Each route calls service function from graph.service.ts
    - electron/api/graph.routes.test.ts exists with 6 test cases
    - `npm test -- electron/api/graph.routes.test.ts -x` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Graph routes created with CRUD endpoints for nodes and edges, all tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Integrate WebSocket handlers with server</name>
  <files>electron/server.ts</files>
  <read_first>
    - electron/server.ts (current implementation with Socket.IO from Plan 01)
    - electron/websocket/socket.handlers.ts (handlers to integrate)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (server integration pattern lines 534-583)
  </read_first>
  <action>
Update electron/server.ts to call setupSocketHandlers(io) after Socket.IO initialization. Import setupSocketHandlers from './websocket/socket.handlers'. Add call after middleware setup and before server.listen(): `setupSocketHandlers(io)`. This registers all WebSocket event handlers ('ai:chat', 'graph:subscribe', 'disconnect'). Pattern from PATTERNS.md lines 534-583 shows setupSocketHandlers(io) call placement. No other changes needed — Socket.IO already integrated in Plan 01.
  </action>
  <acceptance_criteria>
    - electron/server.ts imports setupSocketHandlers from './websocket/socket.handlers'
    - File contains `setupSocketHandlers(io)` call before server.listen()
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>WebSocket handlers integrated with Express server</done>
</task>

<task type="auto">
  <name>Task 4: Register graph routes and add helper for WebSocket emit</name>
  <files>electron/api/routes.ts, electron/websocket/socket.handlers.ts</files>
  <read_first>
    - electron/api/routes.ts (current routes)
    - electron/api/graph.routes.ts (new routes to register)
    - .planning/phases/01-backend-extraction/01-PATTERNS.md (graph update emit pattern lines 302-306)
  </read_first>
  <action>
Update electron/api/routes.ts to register graph routes. Import graphRoutes from './graph.routes'. Add router.use(graphRoutes) with other route registrations from Plan 03. Update electron/websocket/socket.handlers.ts to export emitGraphUpdate(io, graphId, update) helper function per PATTERNS.md lines 302-306. Function: `io.to(\`graph:\${graphId}\`).emit('graph:updated', update)`. This allows graph service to emit updates to subscribed clients. Pattern: graph updates need real-time notification via WebSocket rooms.
  </action>
  <acceptance_criteria>
    - electron/api/routes.ts imports graphRoutes and contains router.use(graphRoutes)
    - electron/websocket/socket.handlers.ts exports emitGraphUpdate function
    - emitGraphUpdate uses io.to() with room name and emits 'graph:updated' event
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>Graph routes registered, WebSocket emit helper created for real-time updates</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| WebSocket client → Socket.IO server | Untrusted browser WebSocket messages cross here |
| AI provider → streaming tokens | External AI API responses cross to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-12 | Tampering | 'ai:chat' event data | mitigate | Validate required fields (conversationId, messages, providerId), check provider config exists |
| T-01-13 | Denial of Service | AI streaming | accept | AI provider rate limits apply, local app has no additional DoS risk |
| T-01-14 | Information Disclosure | WebSocket messages | accept | Local-first app, WebSocket on localhost only, no network exposure |
| T-01-15 | Spoofing | Graph room subscriptions | accept | Local-first app, single user, no authentication needed for localhost |
</threat_model>

<verification>
## Automated Checks

```bash
# WebSocket handler tests pass
npm test -- electron/websocket/socket.handlers.test.ts -x

# Graph route tests pass
npm test -- electron/api/graph.routes.test.ts -x

# TypeScript compilation
npm run build

# Integration test: WebSocket connection and AI streaming (manual in Wave 3)
```

## Manual Verification

- WebSocket connection establishes on server start
- AI chat streams tokens via 'ai:token' events
- Graph updates emit to subscribed clients
- Graph CRUD operations work via HTTP endpoints
</verification>

<success_criteria>
- WebSocket handlers created for AI streaming and graph updates (BACK-02 complete)
- Graph routes created for CRUD operations (BACK-02 complete)
- All IPC handlers converted to HTTP/WebSocket (BACK-02 complete)
- All tests pass (12+ behavior tests across WebSocket and graph routes)
</success_criteria>

<output>
Create `.planning/phases/01-backend-extraction/01-04-SUMMARY.md` when done
</output>
