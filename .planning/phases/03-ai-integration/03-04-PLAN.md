---
phase: 03-ai-integration
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - electron/services/ai/websearch.service.ts
  - tests/websearch.test.ts
  - package.json
autonomous: true
requirements: [AI-08, AI-10]

must_haves:
  truths:
    - Web search queries DuckDuckGo and returns top 5 results per D-14
    - Search results include title, URL, and snippet for each result
    - Search results can be formatted for AI prompt injection
    - Citations can be extracted from search results with position tracking
  artifacts:
    - path: electron/services/ai/websearch.service.ts
      provides: DuckDuckGo search integration and citation extraction
      exports: [WebSearchService, SearchResult]
      min_lines: 80
    - path: tests/websearch.test.ts
      provides: Test coverage for web search operations
      min_lines: 60
  key_links:
    - from: websearch.service.ts
      to: duck-duck-scrape
      via: import { search }
      pattern: import.*search.*from.*duck-duck-scrape
    - from: websearch.service.ts
      to: SearchResult interface
      via: result mapping
      pattern: title.*url.*snippet
---

<objective>
Implement web search integration using DuckDuckGo scraping to provide current information for AI research.

Purpose: Enable AI to research topics using web search per D-13 (built-in web search), fetch top 5 results per D-14, support citation extraction per D-16 through D-19.

Output: WebSearchService with DuckDuckGo integration, search result formatting for AI prompts, citation extraction, comprehensive test coverage with mocked search API.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-RESEARCH.md
@.planning/phases/03-ai-integration/03-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install web search dependencies</name>
  <files>package.json</files>
  
  <read_first>
    - package.json (existing dependencies structure)
    - .planning/phases/03-ai-integration/03-RESEARCH.md (lines 107-112: duck-duck-scrape version and purpose)
  </read_first>
  
  <behavior>
    - Test 1: duck-duck-scrape package appears in package.json dependencies
    - Test 2: npm install completes successfully
  </behavior>
  
  <action>
Install duck-duck-scrape@2.2.7 (verified in RESEARCH.md Package Legitimacy Audit, [OK] disposition):

```bash
npm install duck-duck-scrape@2.2.7
```

This provides free DuckDuckGo search scraping without API keys per D-13 (built-in web search).
  </action>
  
  <verify>
    <automated>grep -q "duck-duck-scrape" package.json && npm list duck-duck-scrape</automated>
  </verify>
  
  <acceptance_criteria>
    - package.json contains "duck-duck-scrape": "2.2.7" in dependencies
    - npm list duck-duck-scrape exits 0 (package installed)
  </acceptance_criteria>
  
  <done>duck-duck-scrape installed and ready for use</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create web search service with DuckDuckGo integration</name>
  <files>electron/services/ai/websearch.service.ts, tests/websearch.test.ts</files>
  
  <read_first>
    - .planning/phases/03-ai-integration/03-RESEARCH.md (lines 545-583: Pattern 6 web search integration, lines 786-799: DuckDuckGo search code example)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (lines 32-35: D-12 through D-15 research flow decisions)
  </read_first>
  
  <behavior>
    - Test 1: search() returns array of SearchResult with title, url, snippet
    - Test 2: search() limits results to maxResults parameter (default 5 per D-14)
    - Test 3: search() returns empty array on error (graceful degradation)
    - Test 4: formatResultsForPrompt() creates numbered citation format [1], [2], [3]
    - Test 5: formatResultsForPrompt() includes title, snippet, and source URL per D-18
    - Test 6: extractCitations() converts SearchResult[] to citation objects with position
  </behavior>
  
  <action>
Create electron/services/ai/websearch.service.ts with DuckDuckGo integration per D-13 (built-in web search), D-14 (fetch top 5 results):

**SearchResult interface:**
```typescript
export interface SearchResult {
  title: string;
  url: string;
  snippet: string;
}
```

**WebSearchService class:**

**search method:**
- Parameters: query: string, maxResults = 5 (per D-14)
- Call duck-duck-scrape's search(query, { safeSearch: 'moderate' })
- Map results to SearchResult interface: { title: r.title, url: r.url, snippet: r.description }
- Slice to maxResults (top 5 by default)
- Return empty array on error (graceful degradation, don't throw)
- Follow pattern from RESEARCH.md lines 556-573

**formatResultsForPrompt method:**
- Parameters: results: SearchResult[]
- Return empty string if results.length === 0
- Format as: "\n\nWeb search results:\n[1] Title\nSnippet\nSource: URL\n\n[2] ..." per D-16 (inline footnotes)
- Numbered citations [1], [2], [3] for AI to reference per D-16
- Follow pattern from RESEARCH.md lines 575-582

**extractCitations method:**
- Parameters: results: SearchResult[]
- Map results to citation objects: { url, title, snippet, position: index + 1 }
- Returns array of citation objects ready for database storage per D-19

Write tests first with mocked duck-duck-scrape (use vitest.mock), then implement.
  </action>
  
  <verify>
    <automated>npm test tests/websearch.test.ts -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - electron/services/ai/websearch.service.ts exports WebSearchService, SearchResult
    - WebSearchService.search uses import { search } from 'duck-duck-scrape'
    - WebSearchService.search limits results to maxResults parameter (default 5)
    - WebSearchService.formatResultsForPrompt creates numbered [1], [2], [3] format
    - WebSearchService.extractCitations returns citation objects with position field
    - tests/websearch.test.ts has 6 tests covering search, formatting, and citation extraction
    - npm test tests/websearch.test.ts exits 0
  </acceptance_criteria>
  
  <done>Web search service implemented with DuckDuckGo integration and citation extraction</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Main → DuckDuckGo | Search queries sent to public DuckDuckGo service |
| DuckDuckGo → Main | HTML responses scraped and parsed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Denial of Service | DuckDuckGo rate limiting | accept | Scraping-based approach may be rate-limited, graceful degradation (return empty results) |
| T-03-15 | Denial of Service | Web search timeout blocks AI | mitigate | 10-second timeout on search, parallel execution with AI generation (Plan 03-06) |
| T-03-16 | Tampering | Malicious content in search results | mitigate | Sanitize snippets before rendering in UI (Plan 03-07) |
| T-03-17 | Information Disclosure | User queries sent to DuckDuckGo | accept | User aware they're using web search, DuckDuckGo doesn't track per privacy policy |
| T-03-SC | Tampering | npm installs (duck-duck-scrape) | mitigate | Package verified in RESEARCH.md legitimacy audit: duck-duck-scrape [OK], 50K+/wk downloads, 3+ years old |
</threat_model>

<verification>
## Phase-Level Checks

1. **Search functionality:** DuckDuckGo search returns results with title, URL, snippet
2. **Result limiting:** Top 5 results returned by default per D-14
3. **Error handling:** Graceful degradation on search failure (empty array)
4. **Prompt formatting:** Results formatted with numbered citations [1], [2], [3]
5. **Citation extraction:** SearchResult[] converted to citation objects with position
6. **Test coverage:** All search operations tested with mocked API
</verification>

<success_criteria>
- Web search queries DuckDuckGo and returns top 5 results
- Search results include title, URL, and snippet
- Results can be formatted for AI prompt injection with numbered citations
- Citations can be extracted with position tracking for database storage
- All web search tests passing
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-04-SUMMARY.md` when done
</output>
