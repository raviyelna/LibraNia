---
phase: 03-ai-integration
plan: 09
type: execute
wave: 5
depends_on: [03-08]
files_modified:
  - src/components/Chat/ChatInterface.tsx
  - src/components/Chat/MessageList.tsx
  - src/components/Chat/MessageInput.tsx
  - src/components/Chat/MessageBubble.tsx
  - src/components/Chat/CitationList.tsx
  - src/components/Chat/ProviderBadge.tsx
  - tests/Chat/ChatInterface.test.tsx
autonomous: true
requirements: [AI-05, AI-06, AI-10]

must_haves:
  truths:
    - User can see chat messages in a scrollable list
    - User can type and send messages through input field
    - Messages display with role-based styling (user vs assistant)
    - Citations display as inline footnotes with clickable links per D-16, D-17
    - Provider name and model display in message metadata per D-06, D-23
    - Progress indicator shows during AI response generation per D-15
  artifacts:
    - path: src/components/Chat/ChatInterface.tsx
      provides: Main chat container component
      min_lines: 100
    - path: src/components/Chat/MessageList.tsx
      provides: Scrollable message list with auto-scroll
      min_lines: 80
    - path: src/components/Chat/MessageInput.tsx
      provides: Message input field with send button
      min_lines: 60
    - path: src/components/Chat/MessageBubble.tsx
      provides: Individual message display with role styling
      min_lines: 80
    - path: src/components/Chat/CitationList.tsx
      provides: Citation display with footnote format
      min_lines: 50
    - path: src/components/Chat/ProviderBadge.tsx
      provides: Provider name and model badge display
      min_lines: 30
  key_links:
    - from: ChatInterface.tsx
      to: MessageList and MessageInput
      via: component composition
      pattern: <MessageList.*<MessageInput
    - from: MessageList.tsx
      to: MessageBubble
      via: map over messages
      pattern: messages\.map.*MessageBubble
    - from: MessageBubble.tsx
      to: CitationList and ProviderBadge
      via: conditional rendering
      pattern: <CitationList.*<ProviderBadge
---

<objective>
Create chat UI components for displaying messages, handling user input, and showing citations with provider metadata.

Purpose: Enable user to interact with AI through chat interface per D-08 (chat in main content area), display citations per D-16/D-17 (inline footnotes with clickable links), show provider transparency per D-06/D-23 (provider name and model), show progress per D-15 (progress indicator).

Output: Six React components for chat UI with role-based styling, citation display, provider badges, and progress indicators.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-PATTERNS.md
@src/components/Notes/NoteEditor.tsx
@src/components/Notes/NotesList.tsx
@src/components/ui/Button.tsx
@src/components/ui/Dialog.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create MessageBubble, CitationList, and ProviderBadge components</name>
  <files>src/components/Chat/MessageBubble.tsx, src/components/Chat/CitationList.tsx, src/components/Chat/ProviderBadge.tsx, tests/Chat/ChatInterface.test.tsx</files>
  
  <read_first>
    - .planning/phases/03-ai-integration/03-PATTERNS.md (lines 489-543: MessageList pattern from NotesList)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-06, D-16, D-17, D-18, D-23: citation and provider display decisions)
    - src/components/Notes/NotesList.tsx (component structure, Tailwind styling)
    - src/components/ui/Button.tsx (UI component patterns)
  </read_first>
  
  <behavior>
    - Test 1: MessageBubble renders user messages with right-aligned styling
    - Test 2: MessageBubble renders assistant messages with left-aligned styling
    - Test 3: MessageBubble displays ProviderBadge for assistant messages only
    - Test 4: CitationList renders footnote links [1], [2], [3] per D-16
    - Test 5: CitationList links are clickable and open in external browser per D-17
    - Test 6: CitationList shows title + domain per D-18 (e.g., "React Docs - react.dev")
    - Test 7: ProviderBadge displays provider name and model per D-06, D-23
  </behavior>
  
  <action>
Create three presentational components following NotesList pattern from PATTERNS.md:

**MessageBubble.tsx:**
- Props: { role: 'user' | 'assistant' | 'system', content: string, provider_id?: string, model?: string, citations?: Citation[], created_at: Date }
- Conditional styling: user messages right-aligned with bg-primary text-primary-foreground, assistant messages left-aligned with bg-muted
- Render content as markdown (use react-markdown if available, or plain text for now)
- Show ProviderBadge for assistant messages only (when provider_id exists)
- Show CitationList below content if citations array has items
- Show timestamp in small text-secondary

**CitationList.tsx:**
- Props: { citations: Citation[] } where Citation = { position: number, url: string, title: string, snippet?: string }
- Render as ordered list with footnote format: [1], [2], [3] per D-16
- Each citation is a clickable link that calls window.api.openExternal(url) per D-17
- Display format: "[N] Title - domain" per D-18 (extract domain from URL)
- Styling: text-sm text-secondary, hover:text-primary for links

**ProviderBadge.tsx:**
- Props: { provider_id: string, model: string }
- Display as small badge: "Claude Sonnet 4" or "GPT-4o" or "DeepSeek Chat" per D-06, D-23
- Styling: inline-flex items-center px-2 py-1 text-xs bg-accent text-accent-foreground rounded
- Map provider_id to display name: claude → "Claude", openai → "GPT", deepseek → "DeepSeek"

Use Tailwind CSS classes following existing component patterns. Create tests/Chat/ directory and add tests for all three components.
  </action>
  
  <verify>
    <automated>npm test tests/Chat/ChatInterface.test.tsx -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - MessageBubble.tsx renders with role-based styling (user right, assistant left)
    - MessageBubble.tsx conditionally renders ProviderBadge for assistant messages
    - MessageBubble.tsx conditionally renders CitationList when citations exist
    - CitationList.tsx renders footnote format [1], [2], [3]
    - CitationList.tsx links call window.api.openExternal on click
    - CitationList.tsx displays "Title - domain" format
    - ProviderBadge.tsx displays provider name and model
    - tests/Chat/ChatInterface.test.tsx has 7 tests covering all behaviors
    - npm test exits 0
  </acceptance_criteria>
  
  <done>Three presentational components created with role styling, citation display, and provider badges</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create MessageInput and MessageList components</name>
  <files>src/components/Chat/MessageInput.tsx, src/components/Chat/MessageList.tsx, tests/Chat/ChatInterface.test.tsx</files>
  
  <read_first>
    - .planning/phases/03-ai-integration/03-PATTERNS.md (lines 489-543: NotesList pattern)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-15: progress indicator)
    - src/components/Notes/NoteEditor.tsx (input handling patterns)
    - src/components/Chat/MessageBubble.tsx (from Task 1)
  </read_first>
  
  <behavior>
    - Test 1: MessageInput renders textarea with send button
    - Test 2: MessageInput calls onSend callback with message text on submit
    - Test 3: MessageInput clears textarea after successful send
    - Test 4: MessageInput disables send button when text is empty or sending
    - Test 5: MessageInput shows progress indicator when sending per D-15
    - Test 6: MessageList renders array of messages using MessageBubble
    - Test 7: MessageList auto-scrolls to bottom when new message arrives
    - Test 8: MessageList shows "Searching web..." / "Generating answer..." during AI response per D-15
  </behavior>
  
  <action>
Create two interactive components:

**MessageInput.tsx:**
- Props: { onSend: (message: string) => Promise<void>, disabled?: boolean, isSending?: boolean }
- Textarea with placeholder "Ask a question..." (auto-resize or fixed height ~80px)
- Send button (use Button component from ui/) disabled when text empty or isSending
- Show progress text "Sending..." when isSending per D-15
- Clear textarea after onSend resolves successfully
- Handle Enter key to send (Shift+Enter for newline)
- Styling: border-t border-border, p-4, flex layout

**MessageList.tsx:**
- Props: { messages: Message[], isGenerating?: boolean, generationStatus?: string }
- Render messages.map(msg => <MessageBubble key={msg.id} {...msg} />)
- Auto-scroll to bottom using useEffect with ref.current?.scrollIntoView({ behavior: 'smooth' })
- Show progress indicator at bottom when isGenerating: generationStatus text (e.g., "Searching web...", "Generating answer...") per D-15
- Styling: flex-1 overflow-y-auto p-4 space-y-4
- Empty state: "Start a conversation by asking a question below"

Add 8 new tests to tests/Chat/ChatInterface.test.tsx covering both components.
  </action>
  
  <verify>
    <automated>npm test tests/Chat/ChatInterface.test.tsx -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - MessageInput.tsx renders textarea and send button
    - MessageInput.tsx calls onSend with message text
    - MessageInput.tsx clears textarea after send
    - MessageInput.tsx disables send when empty or sending
    - MessageInput.tsx shows progress text when isSending
    - MessageList.tsx renders messages using MessageBubble
    - MessageList.tsx auto-scrolls to bottom on new messages
    - MessageList.tsx shows generation status indicator per D-15
    - tests/Chat/ChatInterface.test.tsx has 15 total tests (7 from Task 1 + 8 new)
    - npm test exits 0
  </acceptance_criteria>
  
  <done>MessageInput and MessageList components created with send handling, auto-scroll, and progress indicators</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create ChatInterface container component</name>
  <files>src/components/Chat/ChatInterface.tsx, tests/Chat/ChatInterface.test.tsx</files>
  
  <read_first>
    - .planning/phases/03-ai-integration/03-PATTERNS.md (lines 400-424: component structure with state)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-08: chat in main content area)
    - src/components/Chat/MessageList.tsx (from Task 2)
    - src/components/Chat/MessageInput.tsx (from Task 2)
  </read_first>
  
  <behavior>
    - Test 1: ChatInterface renders MessageList and MessageInput
    - Test 2: ChatInterface accepts conversationId prop and displays that conversation
    - Test 3: ChatInterface shows empty state when no conversationId provided
    - Test 4: ChatInterface passes messages to MessageList
    - Test 5: ChatInterface handles message send from MessageInput
  </behavior>
  
  <action>
Create container component that orchestrates MessageList and MessageInput:

**ChatInterface.tsx:**
- Props: { conversationId?: string }
- Layout: flex flex-col h-full (fills parent container per D-08)
- Compose MessageList (flex-1) and MessageInput (fixed height at bottom)
- Pass conversationId to child components (hooks will be added in Plan 10)
- For now, use placeholder state: const [messages, setMessages] = useState([]) and const [isSending, setIsSending] = useState(false)
- Empty state when no conversationId: "Select a conversation or start a new one"
- Styling: bg-background text-foreground (follows theme)

Add 5 new tests to tests/Chat/ChatInterface.test.tsx covering container behavior.
  </action>
  
  <verify>
    <automated>npm test tests/Chat/ChatInterface.test.tsx -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - ChatInterface.tsx renders MessageList and MessageInput in flex column layout
    - ChatInterface.tsx accepts conversationId prop
    - ChatInterface.tsx shows empty state when no conversationId
    - ChatInterface.tsx fills parent container height per D-08
    - tests/Chat/ChatInterface.test.tsx has 20 total tests (15 from Tasks 1-2 + 5 new)
    - npm test exits 0
  </acceptance_criteria>
  
  <done>ChatInterface container component created with layout and empty state</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User input → MessageInput | User-provided message text enters chat system |
| External links → CitationList | Citation URLs open in external browser |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-28 | Injection | MessageBubble markdown rendering | mitigate | Sanitize markdown content, use safe markdown renderer (react-markdown with default settings) |
| T-03-29 | Spoofing | Citation URLs | mitigate | Validate URL format before opening, use window.api.openExternal (Electron shell.openExternal validates) |
| T-03-30 | Information Disclosure | Provider metadata display | accept | Provider name/model are intentional transparency per D-06, D-23 |
| T-03-SC | Tampering | npm installs | mitigate | react-markdown if used (check Package Legitimacy Gate) |
</threat_model>

<verification>
## Phase-Level Checks

1. **Component structure:** All six components exist and export correctly
2. **Role styling:** User and assistant messages have distinct visual styling
3. **Citation format:** Footnotes display as [1], [2], [3] with clickable links
4. **Provider display:** Provider name and model shown for assistant messages
5. **Progress indicators:** Loading states shown during message send and AI generation
6. **Test coverage:** All component behaviors tested
</verification>

<success_criteria>
- User can see chat messages in scrollable list with role-based styling
- User can type and send messages through input field
- Citations display as inline footnotes with clickable links
- Provider name and model display in message metadata
- Progress indicators show during AI response generation
- All tests passing
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-09-SUMMARY.md` when done
</output>
