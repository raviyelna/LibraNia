---
phase: 03-ai-integration
plan: 11
type: execute
wave: 6
depends_on: [03-09, 03-10]
files_modified:
  - src/routes/Chat.tsx
  - src/App.tsx
  - src/components/Layout/Sidebar.tsx
  - tests/routes/Chat.test.tsx
autonomous: true
requirements: [AI-05, AI-06, AI-10]

must_haves:
  truths:
    - User can navigate to Chat route from sidebar
    - Chat route displays conversation list in sidebar per D-10
    - Chat route displays ChatInterface in main content area per D-08
    - User can select a conversation from list to view messages
    - User can start a new conversation
    - Chat interface fills main content area and replaces note editor when active per D-08
  artifacts:
    - path: src/routes/Chat.tsx
      provides: Chat route with conversation list and chat interface
      min_lines: 150
    - path: src/App.tsx
      provides: Chat route registration
      contains: <Route path="/chat"
    - path: src/components/Layout/Sidebar.tsx
      provides: Chat navigation link
      contains: to="/chat"
    - path: tests/routes/Chat.test.tsx
      provides: Test coverage for chat route
      min_lines: 100
  key_links:
    - from: Chat.tsx
      to: useConversations hook
      via: conversation list state
      pattern: useConversations\(\)
    - from: Chat.tsx
      to: ChatInterface component
      via: component composition
      pattern: <ChatInterface.*conversationId
    - from: Sidebar.tsx
      to: Chat route
      via: navigation link
      pattern: <Link.*to="/chat"
---

<objective>
Integrate chat UI into application with route, navigation, and conversation list sidebar.

Purpose: Enable user to access chat interface per D-08 (chat in main content area), browse conversations per D-10 (conversation list in sidebar), navigate between conversations, and start new conversations.

Output: Chat route with three-column layout (conversation list sidebar + chat interface + optional details panel), navigation link in sidebar, route registration in App.tsx.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-PATTERNS.md
@src/routes/Library.tsx
@src/App.tsx
@src/components/Layout/Sidebar.tsx
@src/components/Chat/ChatInterface.tsx
@src/hooks/useConversations.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Chat route with conversation list and interface</name>
  <files>src/routes/Chat.tsx, tests/routes/Chat.test.tsx</files>
  
  <read_first>
    - .planning/phases/03-ai-integration/03-PATTERNS.md (lines 548-582: Library.tsx three-column layout)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-08, D-10, D-11: chat layout and conversation list)
    - src/routes/Library.tsx (route structure, layout pattern)
    - src/components/Chat/ChatInterface.tsx (from Plan 09)
    - src/hooks/useConversations.ts (from Plan 10)
  </read_first>
  
  <behavior>
    - Test 1: Chat route renders conversation list sidebar
    - Test 2: Chat route renders ChatInterface in main content area
    - Test 3: Chat route loads conversations using useConversations hook
    - Test 4: Selecting conversation updates ChatInterface with conversationId
    - Test 5: "New Conversation" button creates new conversation
    - Test 6: Empty state shows when no conversations exist
    - Test 7: Conversation list shows auto-generated titles per D-11
  </behavior>
  
  <action>
Create Chat route following Library.tsx pattern from PATTERNS.md (lines 548-582):

**Chat.tsx:**

Layout structure:
- Three-column flex layout: conversation list sidebar (w-64) + main chat area (flex-1) + optional details panel
- Use useState for selectedConversationId
- Use useConversations() hook to load conversation list

**Conversation list sidebar (left):**
- Header with "Conversations" title and "New Conversation" button
- List of conversations from useConversations hook
- Each conversation item shows:
  - Auto-generated title (first 50 chars of first message per D-11)
  - Last updated timestamp
  - Selected state styling (bg-accent when selected)
- Click conversation to setSelectedConversationId
- Empty state: "No conversations yet. Start a new one!"
- Styling: border-r border-border overflow-y-auto

**Main chat area (center):**
- Render <ChatInterface conversationId={selectedConversationId} />
- Empty state when no conversation selected: "Select a conversation or start a new one"
- Fills remaining space with flex-1

**New conversation logic:**
- "New Conversation" button calls window.api.conversation.create({ title: 'New Conversation' })
- After creation, setSelectedConversationId to new conversation ID
- Refetch conversation list

Follow exact layout pattern from Library.tsx (lines 554-578 in PATTERNS.md). Create tests/routes/ directory and add 7 tests.
  </action>
  
  <verify>
    <automated>npm test tests/routes/Chat.test.tsx -- --run</automated>
  </verify>
  
  <acceptance_criteria>
    - Chat.tsx renders three-column layout per D-08, D-10
    - Conversation list sidebar shows all conversations with auto-generated titles per D-11
    - ChatInterface displays in main content area
    - Selecting conversation updates ChatInterface with conversationId
    - "New Conversation" button creates conversation and selects it
    - Empty states handled for no conversations and no selection
    - tests/routes/Chat.test.tsx has 7 tests covering all behaviors
    - npm test exits 0
  </acceptance_criteria>
  
  <done>Chat route created with conversation list sidebar and chat interface integration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Chat navigation link and route registration</name>
  <files>src/components/Layout/Sidebar.tsx, src/App.tsx, tests/routes/Chat.test.tsx</files>
  
  <read_first>
    - src/components/Layout/Sidebar.tsx (existing navigation links)
    - src/App.tsx (existing route registration)
    - src/routes/Chat.tsx (from Task 1)
  </read_first>
  
  <behavior>
    - Test 1: Sidebar contains "Chat" navigation link
    - Test 2: Chat link navigates to /chat route
    - Test 3: Chat link shows active state when on /chat route
    - Test 4: App.tsx registers /chat route with Chat component
    - Test 5: Navigating to /chat renders Chat route
  </behavior>
  
  <action>
Update navigation and routing:

**Sidebar.tsx:**
- Add "Chat" link after "Library" link (or before, based on existing order)
- Use same Link component pattern as existing navigation
- Icon: MessageSquare or similar from lucide-react (if available) or text-only
- Active state styling: bg-accent when pathname === '/chat'
- Label: "Chat"

**App.tsx:**
- Import Chat component: import { Chat } from './routes/Chat'
- Add route: <Route path="/chat" element={<Chat />} />
- Place after Library route, before Settings route (or based on existing order)

Follow existing patterns in both files. Add 5 new tests to tests/routes/Chat.test.tsx (12 total).
  </action>
  
  <verify>
    <automated>npm test tests/routes/Chat.test.tsx -- --run && npx tsc --noEmit</automated>
  </verify>
  
  <acceptance_criteria>
    - Sidebar.tsx contains "Chat" navigation link
    - Chat link navigates to /chat route
    - Chat link shows active state when on /chat
    - App.tsx registers /chat route with Chat component
    - tests/routes/Chat.test.tsx has 12 total tests (7 from Task 1 + 5 new)
    - npm test exits 0
    - npx tsc --noEmit exits 0
  </acceptance_criteria>
  
  <done>Chat navigation link added to sidebar and route registered in App.tsx</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User navigation → Chat route | User accesses chat interface via sidebar navigation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-33 | Information Disclosure | Conversation list display | accept | Conversation titles are user's own data, displayed intentionally |
| T-03-34 | Denial of Service | Large conversation list | mitigate | Implement virtual scrolling if >100 conversations (defer to future if not needed now) |
| T-03-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
## Phase-Level Checks

1. **Route registration:** /chat route exists and renders Chat component
2. **Navigation:** Sidebar contains Chat link with active state
3. **Layout:** Three-column layout matches Library.tsx pattern
4. **Conversation list:** Displays all conversations with auto-generated titles
5. **Chat interface:** Renders in main content area with selected conversation
6. **New conversation:** Button creates conversation and selects it
7. **Test coverage:** All route and navigation behaviors tested
</verification>

<success_criteria>
- User can navigate to Chat route from sidebar
- Chat route displays conversation list in sidebar
- Chat route displays ChatInterface in main content area
- User can select conversations from list to view messages
- User can start new conversations
- Chat interface fills main content area per D-08
- All tests passing
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-11-SUMMARY.md` when done
</output>
