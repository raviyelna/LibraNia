---
phase: 03-ai-integration
plan: 08
type: execute
wave: 4
depends_on: [03-05]
files_modified:
  - electron/main.ts
  - electron/preload.ts
  - src/vite-env.d.ts
autonomous: true
requirements: [AI-05, AI-06]

must_haves:
  truths:
    - IPC handlers expose chat operations to renderer process
    - Renderer can call chat and conversation operations via window.api
    - TypeScript types ensure type-safe IPC communication
  artifacts:
    - path: electron/main.ts
      provides: IPC handler registration
      min_lines: 5
    - path: electron/preload.ts
      provides: contextBridge exposure of chat APIs
      min_lines: 30
    - path: src/vite-env.d.ts
      provides: TypeScript definitions for chat APIs
      min_lines: 40
  key_links:
    - from: main.ts
      to: ai.handlers.ts
      via: registerAIHandlers call
      pattern: registerAIHandlers\(mainWindow\)
    - from: preload.ts
      to: renderer
      via: contextBridge.exposeInMainWorld
      pattern: contextBridge\.exposeInMainWorld
---

<objective>
Register AI IPC handlers in main process and expose chat APIs to renderer with TypeScript types.

Purpose: Complete IPC integration for chat operations, enable renderer to call AI services, ensure type-safe communication.

Output: IPC handlers registered in main process, chat APIs exposed via contextBridge, TypeScript definitions for type safety.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-ai-integration/03-CONTEXT.md
@.planning/phases/03-ai-integration/03-PATTERNS.md
@electron/main.ts
@electron/preload.ts
@src/vite-env.d.ts
@electron/ipc/ai.handlers.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register IPC handlers and expose to renderer</name>
  <files>electron/main.ts, electron/preload.ts, src/vite-env.d.ts</files>
  
  <read_first>
    - electron/main.ts (existing handler registration pattern)
    - electron/preload.ts (existing contextBridge pattern)
    - src/vite-env.d.ts (existing API type definitions)
    - electron/ipc/ai.handlers.ts (new handlers from Plan 03-05)
  </read_first>
  
  <action>
Update three files to expose AI IPC handlers to renderer:

**electron/main.ts:**
- Import registerAIHandlers from './ipc/ai.handlers'
- Call registerAIHandlers(mainWindow) after registerNotesHandlers() in app.whenReady

**electron/preload.ts:**
- Add chat and conversation APIs to contextBridge.exposeInMainWorld('api', { ... })
- chat: { send, summarizeNote, onToken, offToken }
- conversation: { getAll, get, delete }
- onToken/offToken use ipcRenderer.on/off for streaming events per Pattern 2

**src/vite-env.d.ts:**
- Add ChatAPI and ConversationAPI interfaces
- Extend Window interface with api.chat and api.conversation

Follow existing pattern from notes/links APIs in Phase 2.
  </action>
  
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  
  <acceptance_criteria>
    - electron/main.ts calls registerAIHandlers(mainWindow) after initDatabase
    - electron/preload.ts exposes window.api.chat with send, summarizeNote, onToken, offToken
    - electron/preload.ts exposes window.api.conversation with getAll, get, delete
    - src/vite-env.d.ts defines ChatAPI and ConversationAPI interfaces
    - npx tsc --noEmit exits 0 (TypeScript compilation passes)
  </acceptance_criteria>
  
  <done>IPC handlers registered and exposed to renderer with TypeScript types</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Renderer → Main (IPC) | Chat operations cross from renderer to main via contextBridge |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-27 | Tampering | IPC message tampering | mitigate | contextBridge isolates renderer, IPC handlers validate inputs |
| T-03-SC | Tampering | npm installs | mitigate | No new packages in this plan |
</threat_model>

<verification>
## Phase-Level Checks

1. **IPC registration:** registerAIHandlers called in main.ts
2. **API exposure:** chat and conversation APIs exposed via contextBridge
3. **Type safety:** TypeScript definitions ensure type-safe IPC calls
4. **Compilation:** npx tsc --noEmit passes
</verification>

<success_criteria>
- IPC handlers registered in main process
- Chat APIs exposed to renderer via window.api
- TypeScript types ensure type-safe communication
- TypeScript compilation passes
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-08-SUMMARY.md` when done
</output>
