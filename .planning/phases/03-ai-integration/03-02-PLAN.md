---
phase: 03-ai-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - bin/librania.js
  - package.json
autonomous: true
requirements:
  - CLI-02
  - CLI-05
  - CLI-06

must_haves:
  truths:
    - Server auto-opens default browser to web UI unless --no-browser flag used
    - Browser opens after server is ready (no connection refused errors)
    - Server shuts down gracefully on Ctrl+C (closes connections, flushes logs)
    - User can run headless with --no-browser flag
  artifacts:
    - path: bin/librania.js
      provides: Browser launch logic and graceful shutdown
      min_lines: 80
    - path: package.json
      provides: open package dependency
      contains: '"open"'
  key_links:
    - from: bin/librania.js
      to: open package
      via: import and call after server ready
      pattern: "import.*open"
    - from: bin/librania.js
      to: electron/server.ts stopServer
      via: SIGINT handler
      pattern: "stopServer"

---

<objective>
Add browser auto-launch and graceful shutdown to CLI, completing the single-command user experience.

Purpose: Enable "run and go" workflow where `librania start` launches server and opens browser automatically. Ensure clean shutdown on Ctrl+C to prevent data corruption and orphaned processes.

Output: CLI that auto-opens browser after server starts and handles SIGINT/SIGTERM gracefully.
</objective>

<execution_context>
@/home/user1/LibraNia/.claude/get-shit-done/workflows/execute-plan.md
@/home/user1/LibraNia/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/user1/LibraNia/.planning/PROJECT.md
@/home/user1/LibraNia/.planning/ROADMAP.md
@/home/user1/LibraNia/.planning/STATE.md
@/home/user1/LibraNia/.planning/phases/03-ai-integration/03-CONTEXT.md
@/home/user1/LibraNia/.planning/phases/03-ai-integration/03-RESEARCH.md
@/home/user1/LibraNia/electron/server.ts
@/home/user1/LibraNia/start-server.js
@/home/user1/LibraNia/bin/librania.js
</context>

<interfaces>
<!-- Key interfaces from existing codebase -->

From electron/server.ts:
```typescript
export interface ServerInstance {
  server: http.Server;
  io: SocketIOServer;
  port: number;
}

export async function startServer(
  port: number,
  distPath: string
): Promise<ServerInstance>

export function stopServer(instance: ServerInstance): Promise<void>
```

From start-server.js (graceful shutdown pattern):
```javascript
// Handle graceful shutdown
process.on('SIGINT', () => {
  console.log('\nShutting down server...');
  process.exit(0);
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Install open package for cross-platform browser launch</name>
  <files>package.json</files>
  <read_first>
    - package.json (check existing dependencies)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-06: open package decision)
  </read_first>
  <action>
    Install open package version ^10.1.0 (latest stable, 20M+ weekly downloads per D-06). Run `npm install open@^10.1.0` to add to dependencies section. Open package handles OS-specific browser commands (xdg-open on Linux, start on Windows, open on macOS) and provides cross-platform API for launching default browser.
  </action>
  <verify>
    <automated>grep -c '"open"' package.json</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains `"open": "^10.1.0"` in dependencies section
    - `npm list open` shows open@10.1.0 installed
  </acceptance_criteria>
  <done>open package installed and available for browser launch</done>
</task>

<task type="auto">
  <name>Task 2: Add browser auto-launch after server ready</name>
  <files>bin/librania.js</files>
  <read_first>
    - bin/librania.js (existing startServer call, port handling)
    - electron/server.ts (startServer Promise resolution, port in ServerInstance)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-05 through D-08, D-11)
  </read_first>
  <action>
    Import open from 'open' package at top of bin/librania.js. Add `--no-browser` boolean flag to commander start command (per D-05, CLI-05). After startServer() Promise resolves, check if browser flag is true (default true per D-05). If true, call `await open(`http://localhost:${actualPort}`)` where actualPort comes from ServerInstance.port (per D-07, wait for server ready). Wrap open() call in try-catch and log warning on failure without exiting (per D-08, non-blocking). Log clean startup message: "LibraNia server running at http://localhost:{port}" and "Press Ctrl+C to stop" (per D-11). If --no-browser flag used, log "Server running in headless mode" instead of opening browser.
  </action>
  <verify>
    <automated>grep -c 'import.*open' bin/librania.js && grep -c 'no-browser' bin/librania.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/librania.js imports open from 'open' package
    - File contains `--no-browser` flag definition in commander
    - File calls open() after startServer() resolves with actualPort
    - open() call wrapped in try-catch with warning log on failure
    - File logs "LibraNia server running at http://localhost:{port}"
    - File logs "Press Ctrl+C to stop"
    - Browser launch skipped when --no-browser flag present
  </acceptance_criteria>
  <done>Browser auto-launch implemented with --no-browser flag support</done>
</task>

<task type="auto">
  <name>Task 3: Implement graceful shutdown on SIGINT/SIGTERM</name>
  <files>bin/librania.js</files>
  <read_first>
    - bin/librania.js (existing startServer call, ServerInstance variable)
    - electron/server.ts (stopServer function signature)
    - start-server.js (SIGINT handler pattern)
    - .planning/phases/03-ai-integration/03-CONTEXT.md (D-10: graceful shutdown requirements)
  </read_first>
  <action>
    Store ServerInstance returned from startServer() in module-level variable (e.g., `let serverInstance`). Add process.on('SIGINT') handler that logs "\nShutting down gracefully..." (per D-10). Inside handler, check if serverInstance exists, then call `await stopServer(serverInstance)` which closes HTTP server and Socket.IO connections (per D-10). After stopServer completes, call process.exit(0). Add identical handler for process.on('SIGTERM') to handle kill signals. Wrap stopServer in try-catch and log error if shutdown fails, then force exit with process.exit(1). Add timeout (5 seconds) for shutdown - if stopServer doesn't complete in 5s, log warning and force exit to prevent hanging.
  </action>
  <verify>
    <automated>grep -c "process.on('SIGINT'" bin/librania.js && grep -c 'stopServer' bin/librania.js</automated>
  </verify>
  <acceptance_criteria>
    - bin/librania.js stores ServerInstance in module-level variable
    - File contains process.on('SIGINT') handler
    - File contains process.on('SIGTERM') handler
    - Handlers call stopServer(serverInstance) before process.exit(0)
    - stopServer call wrapped in try-catch with error logging
    - Shutdown has 5-second timeout to prevent hanging
    - Handler logs "Shutting down gracefully..." message
  </acceptance_criteria>
  <done>Graceful shutdown implemented for SIGINT and SIGTERM signals</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → Browser | open() launches external browser with localhost URL |
| Signal handler → Server | SIGINT/SIGTERM triggers shutdown sequence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | Browser launch URL | accept | URL is localhost only, no user input in URL construction |
| T-03-05 | Denial of Service | Shutdown timeout | mitigate | 5-second timeout prevents hanging, force exit after timeout |
| T-03-06 | Information Disclosure | Startup logs | accept | Logs reveal only port number (localhost), no sensitive data |
| T-03-SC | Tampering | npm install open | mitigate | open@^10.1.0 verified on npm registry, no slopcheck needed (established package) |
</threat_model>

<verification>
## Manual Verification Steps

1. Test browser auto-launch (default):
   ```bash
   npm run start:cli
   # Expected: Server starts, browser opens to http://localhost:3000
   ```

2. Test headless mode:
   ```bash
   npm run start:cli -- --no-browser
   # Expected: Server starts, no browser opens, logs "headless mode"
   ```

3. Test graceful shutdown:
   ```bash
   npm run start:cli
   # Press Ctrl+C
   # Expected: Logs "Shutting down gracefully...", server stops cleanly
   ```

4. Test browser launch failure handling:
   ```bash
   # Simulate failure by setting invalid BROWSER env var
   BROWSER=/nonexistent npm run start:cli
   # Expected: Warning logged, server continues running
   ```

5. Test custom port with browser:
   ```bash
   npm run start:cli -- --port 3001
   # Expected: Browser opens to http://localhost:3001
   ```

## Automated Verification

```bash
npm test -- --run
```
</verification>

<success_criteria>
- [ ] User runs `librania start` and browser opens automatically to http://localhost:3000
- [ ] User runs `librania start --no-browser` and server runs headless
- [ ] User presses Ctrl+C and server shuts down gracefully (logs message, closes connections)
- [ ] Browser launch failure logs warning but doesn't crash server
- [ ] Shutdown completes within 5 seconds or force exits
- [ ] SIGTERM signal also triggers graceful shutdown
- [ ] Startup logs show clean message with URL and "Press Ctrl+C to stop"
</success_criteria>

<output>
Create `.planning/phases/03-ai-integration/03-02-SUMMARY.md` when done
</output>
